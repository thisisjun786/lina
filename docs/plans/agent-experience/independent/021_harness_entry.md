# Harness entry and source revalidation

Kernel D concluded that the deterministic standalone protocol is implemented, with independent PASS and 60 tests; it did not measure model utility. This unit follows the unchanged next direction in 015_kernel_result.md: implement raw model transport and matched evaluation. No production engine changes or dependencies are needed.

Loop: satisfy-spec. Trigger: registered harness work phase following kernel D. Goal: a CLI that exports blinded synthetic cases, runs raw-model/kernel/understanding-ablation modes with common budgets, and scores deterministic outputs in a separate evaluator process. Non-goals: process-crash qualification and final three-batch acceptance belong to following phases; no production integration, publication or installation. Stop this unit only after transport/runner/scorer/parity/negative-control checks and independent review pass. Record plan decisions here and in 020_harness.md, evidence in the session evidence directory. Missing permissions or service errors retain failure evidence; main resolves implementation uncertainty and reclaims a delegated slice after two distinct failed agents. New external write scope requires user authority. User authorized no overall token/call/time cap; each episode remains six requests, each response 4096 tokens, sequential or bounded model concurrency, per-request timeout and cancellation.

Verified source at entry: kernel implementation db65d2e; final docs 7c5f03e; C test receipt ran `sh /tmp/lina-independent-kernel/check.sh` and exited0. Its Bun command directly names `./scripts/qa/adoption-kernel`, and local tsconfig includes `./**/*.ts`, so both will observe new harness files. Biome directly names the same directory. CLI commands are planned new targets, not yet existing verifiers. No harness source files were present at entry. Existing owners are 020_harness.md, 001_rubric.md and 005_scoring_contract.md; rubric hashes stay frozen.

Creation/serialization/consumer chains to make concrete before A:

- Transport config (endpoint/model/key/timeout/max output) enters CLI/environment, makes an HTTP request with the shared system/schema/input serializer, decodes content and usage, records synthetic request/response and redacted transport metadata. Credentials never enter artifacts. Unknown HTTP/transport failures remain distinguishable from invalid model output; no silent provider fallback or retries.
- PublicCase contains an opaque episode ID, purpose/staged raw events, tool catalog and synthetic environment inputs. Exporter generates it separately from PrivateTruth. Runner accepts only the public file; score process reads truth plus recorded trace. Criterion IDs, seeds and expected outcomes stay in private mapping. Both file decoders reject unknown versions and malformed payloads.
- RunMode baseline/kernel/ablation enters CLI and run metadata. All modes use the same system, action schema, raw history/events, tool environment and request budgets. One serializer projects the optional derived block; ablation removes understanding only. Baseline records raw messages/proposals/results but does not apply adoption derivation. All mode consumers and output/report readers must handle the same three values explicitly.
- EpisodeTrace contains actual model calls, usage, proposals, tool admissions/results, delivered structured answers, raw-event revisions, and actual input adoption IDs. Scorer checks 005 predicates against independent truth. Kernel uptake is separate from common task quality for baseline comparison. All 30 rows required for qualification; absent host-process evidence is incomplete, not manufactured pass.

Architect consultation is running (dispatch harness-architect, child 01a08785-29df-7fa2-a848-5cc82cf67605). Main requested stable responsibility/interface decisions and a principled receipt-citation bridge: B11/B12 need adopted understanding to cite failed-quality receipts, while the kernel currently accepts only evidence/adoption refs. No implementation begins until this is resolved in the executable plan and reflected by the same architect.

Planned delegation boundary: executor may implement raw transport/shared serialization/tests in model.ts and dedicated model tests; main owns scenario generation, private-truth export, scorer and runner. Exact shared signatures are frozen in P before dispatch, so neither task changes the other's files. No additional engine/framework or test-only runtime branch is allowed.

Planned activation proof: HTTP malformed/timeout/error preserves explicit failure; invalid action never becomes default answer; missing score row fails; private evaluator canary never reaches serialized requests; identical-history mode inputs match after derived metadata removal; ablation keeps raw evidence and removes understanding only; baseline has no derived state; always-defer/always-submit/stale-copy controls fail their designated predicates; cap omission generates explicit omitted-input metadata. The evaluator process boundary is executable isolation by separate input paths, not protection against a trusted caller deliberately handing private truth to the runner; report that residual limit plainly.

Main interface candidates for architect reflection (not frozen implementation): a transport accepts already serialized common messages and returns raw content plus usage/error metadata; the runner owns episode control rather than hiding retries in ModelPort. Public scenario stages are ordinary user instructions/events, and stage advancement must depend only on public requested action/terminal state, not private criterion truth. Behavioral answer JSON remains exactly 005_scoring_contract.md. Tool owners deterministically implement lookup/calculate/check/submit over public synthetic environment data; expected answers are calculated separately in private truth. Prelude tool receipts, if used to establish prior failure, must be created by the same synthetic owner for all modes and recorded/costed explicitly, never invented inside the scorer.

Scoring output separates common task quality from kernel-specific uptake for B11/B12/B13. Unknown transport makes a qualification attempt incomplete and retains it; malformed model output is a failed trial. The scorer must not silently discard either from an apparent passing batch. Numerical rows use independent values/sets/receipt identities, not free-text keywords or the model's own rating. Natural language review is supplemental.

Second integration issue sent to architect: the kernel ModelPort receives a filtered current Frame, while the baseline is described as raw transcript persistence and 020 requires identical-history parity after removing derived metadata. Resolve this explicitly before A. A raw-history channel must not bypass subject/privacy fences or escape the actual-input dependency fence; updating one transcript Evidence on every turn must not invalidate all prior adoptions. The parity test must include correction and prior tool-result histories, not only an initial all-current input. Any clarification must preserve the frozen comparison and cannot be a hidden advantage or rubric weakening.
