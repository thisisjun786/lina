# Standalone kernel result

The deterministic standalone kernel is implemented and independently reviewed. It uses a model proposal port, SQLite state, and a separate synthetic delivery owner, without importing Lina's memory, persona, context, LIFE, channels or execution adapters. A runnable entry for this unit is `bun test ./scripts/qa/adoption-kernel`; user CLI and real model evaluation are the next unit.

The kernel canonicalizes source identity, preserves revisions and original judgment snapshots, invalidates dependent understanding/plans/intentions after correction, filters subject/private data before model input, validates model proposals, and fences tool/delivery admission against current dependencies and permissions. Durable request reservations prevent duplicate execution; reconciliation preserves completed results and leaves uncertainty explicit. Deferred wake and cancelled work recover without duplicate owner calls.

Independent review PASS at db65d2ef2436506f612d7b16e4fc26c04a758784 closed six substantive findings plus the cancellation recovery boundary, with accepted synthesis in 013_kernel_review.md. C ran `cxc receipt test --session 01a086fb-5429-79a2-8326-3def2b53dc27 -- sh /tmp/lina-independent-kernel/check.sh`: 60 tests passed, strict local TypeScript passed, Biome exited0 with informational indexed-key suggestions, and git diff check passed. Evidence: `.codexclaw/evidence/01a086fb-5429-79a2-8326-3def2b53dc27/kernel-C.log` and `test-receipt.json`. 014_kernel_checks.md maps the H01-H10 activation evidence. All repair RED logs are retained; the executor's original misleading red-named green log is not claimed as TDD.

This unit does not establish OS-process crash recovery, live model quality, baseline advantage, or the 30-row qualification threshold. Unknown model-only calls stay held rather than silently retried. Frames conservatively retain all supplied dependency ancestry, so irrelevant-source changes may invalidate a derivation; semantic condition selection remains model work. No production integration, remote publication, deployment or dependency installation occurred.

Next direction: revalidate 020_harness.md, implement raw Ollama transport and the equal-information baseline/kernel/ablation runner with evaluator truth kept in a separate process. Follow with the registered process-recovery unit and three fresh qualification batches. No hypothesis about model gains has been tested; any missing gain or plateau must be reported honestly without lowering the frozen rubric.
