# Roadmap audit synthesis

Independent reviewer returned FAIL with seven material blockers. A remains open. No implementation exists. Accept B1-B4, B6-B7; accept B5 parity objective with the qualification below.

1. Provenance schema omitted source authority, participant role, expectation and quality. Add explicit contracts in 010, preserve them through validated SQLite JSON and input projection.
2. Proposal validation alone leaves dispatch race. Add same-transaction reservation and final dependency fence, plus controlled race evidence. Same-process synthetic ports synchronously admit effects before yielding; real distributed effects require a future owner-side fence, not claimed here.
3. Private derivations and delivery have no owner. Add inherited classification and an actual delivery port; public requests, public traces and delivery consume only eligible projected inputs.
4. Tool completion/kernel-write crash requires receipt lookup. Add owner reconcile and a separate durable synthetic effect owner with the missing crash milestone.
5. Use one serializer, system policy and runner. Exact byte parity applies to identical histories; after policies select different actions, result histories necessarily differ. Compare common configuration and shared raw source opportunities, and replay identical histories through all serializers. Do not force artificial equality of post-action observations. Baseline remains raw model+tools, not secretly kernel-enforced; kernel-versus-ablation isolates only derived-understanding consumption.
6. Move private expected truth into evaluator-only files/process; runner receives allowlisted public fields. Canary scanning excludes naturally shared correct numeric answer substrings, which necessarily occur in source data; unique evaluator canaries/IDs and full private objects must never cross.
7. Freeze exact per-row machine predicates in 005 before qualification. Free natural-language quality is separately reported by blinded review, never inferred from keyword matches or added to numeric scores. Every numeric behavior trial uses a declared structured result plus externally verified effect/trace predicates. This limits claims to task families; broader semantic competence remains unproven.

No threshold lowered, no row removed. Parent design still governs original meanings; this independent experiment is the current user-authorized scope.

Round 2 FAIL: accept two remaining vacuity findings. B12 now requires an existing active understanding and actual frame inclusion before checking non-generalization; remove prose-only unconditional-claim predicate in favor of structured claim exclusions. B13 now requires exact pre-correction active revision1 plan/frame proof and checks the same plan ID becomes ineligible. Missing antecedent is a failed kernel trial. Thresholds unchanged.

Round 3: reviewer 01a08732-8929-7643-a054-2bd101155cca confirmed no blockers, VERDICT: PASS. This is plan sufficiency only. Implementation not started.
