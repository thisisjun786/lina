# Kernel progress, incomplete

Executor delivered six draft modules, then main inspected and added durable SQLite delivery owner with conflicting-replay rejection. The executor's budget-exhausted label is not a goal budget stop; no overall budget was set. Its first file named red contains green output and is explicitly invalid RED evidence. Preserve it without claiming TDD. Main delivery-red.log shows missing close API failure before durable delivery implementation; delivery-green.log and the local check launcher show 18 pass, strict TypeScript and Biome success.

Remaining before kernel completion: validated restore for all stored rows; decision method/expectation/snapshot retention on every transition; atomic final admission under concurrent writers; durable answer decision/effect tracking; meaningful receipt reconciliation and consumption recovery; exact deferred resume and duplicate decision identity; full H01-H10 negatives including derived private ancestry/receipt projection. Some current test names mention H12/H14/H15 but only narrow cases are covered, not full qualification. Real process H11-H13 tests belong to recovery after kernel contracts exist. Current phase stays B; no full kernel completion claim.

Main now owns all experiment files; executor was closed after delivery. No provider benchmark calls since transport smoke. Continue with failing tests for remaining audited contract gaps, then independent code review and C. Do not move to model harness until kernel D.

Main follow-up: validated evidence/purpose/adoption row shape and SQLite id/revision on read (restore-red: 18 pass/2 fail -> 20 pass); durable receipt projection now requires matching subject, permissible source-frame classification and current supplied refs (receipt-scope-red: 20 pass/1 fail -> 21 pass). `receipt-scope-verified.log` includes 21 tests, strict types and Biome pass. These do not complete effect/decision-row decoding, transitive raw-evidence parents, atomic admission or recovery. Main preserves build phase B until full kernel contracts pass.

Resume follow-up: pending tool effects now ask original owner.reconcile(effectId), leave null/unknown unresolved, reject mismatched receipt IDs, and save completed/failed receipts without model calls or readmission. Reconcile RED 21 pass/1 fail -> GREEN 22 pass, strict types/Biome pass. This is in-process owner recovery only; process crash, durable result consumption, delivery replay and concurrent reservation remain unverified. No H11-H13 completion claim.

Additional repairs: explicit unknown tool receipts stay unresolved; malformed quality values are rejected on persistence; original decision frame survives adoption. Adoption record and decision status update now share BEGIN IMMEDIATE/COMMIT with rollback on stale state or write failure. decision-preservation-red shows 24 pass/1 fail before fix; green shows 25 pass/0 fail and strict types, Biome exit0 (one literal-key informational diagnostic from strict indexed test access). Method/expectation fields and remaining execution fences still need implementation; no kernel D claim.

Ancestor repair: raw evidence now checks exact parent revisions recursively, ownership/public projection and cycle exclusion; ancestor-red 25/1 -> ancestor-green 26/0. Prepared decision records explicitly preserve expectation none, method null and policyVersion rather than inferring a past prediction from later outcome (expectation-red -> green). Explicit stated expectations/method selection remain to be added to proposal/decision contract. Current checks 26 pass, strict types and Biome exit0; no whole-kernel completion claim.

Judgment contract now supports optional model `judgment` {method, expectation:none|stated} on every proposal action. Parser validates it, kernel fixes it before adoption/dispatch, store preserves the exact prior expectation and disallows later rewriting. Missing judgment stays method:null/expectation:none. Regression judgment-red 26 pass/1 fail -> 27 pass, typecheck and Biome exit0. Remaining: concurrency protection for judgment write/final effect admission, durable answer tracking and terminal decisions, proper signaled deferred execution, complete effect/decision restoration and actual process recovery.

Answer effect tracking: final answer now saves an effect marker before delivery, reads owner receipt afterward, and resume reconciles the original answer ID without readmission. Injected failure immediately after owner commit: answer-recovery-red 27/1 -> verified 28/0; strict types/Biome exit0. Recovery owner selected by host-created effect ID suffix, not user tool name. Actual process kill/restart, atomic dispatch/correction across connections, duplicate decision suppression and deferred wake remain open.

Deferred wake now actually invokes a new current-purpose judgment after matching signal and links completed wake to next decision. An older fixture always returns defer; its assertion was corrected to require a distinct new deferred decision, not falsely forbid valid re-deferral. Concurrent wakes and crash-between-child-and-parent-link remain open. Final synchronous owner admission now executes inside BEGIN IMMEDIATE with current frame recheck; durable effect marker is committed before this transaction, so an owner exception cannot erase uncertainty. Two SQLite connections test blocks competing correction during admission then permits it after release. fence-red 29/1 -> fence-green 30/0; strict types/Biome exit0. Thirty unit tests are not the thirty live qualification criteria.

Durable run reservations now serialize judgments per purpose and replay a completed request after SQLite reopen. Unknown calls remain reserved, with no implicit retry. Reservation RED 30/1 -> GREEN 31/0; reopen 32/0. The model receives a separate frame copy so changing its input cannot alter the final admission snapshot (snapshot-mutation RED 32/1 -> GREEN 33/0; the original failure was an exception, not a delivered stale answer). Deferred wake uses a stable request identity and idempotent parent link, preserving the completed child across an injected interruption and concurrent resumption (wake-link and wake-concurrent RED -> GREEN 34/0).

Restored run traces now validate decision identity, trace shape and terminal/unknown status consistency (run-restore RED 34/2 -> GREEN 36/0). Domain records validate before insertion, and adoption eligibility selects the latest revision before filtering withdrawn records (current-record RED 36/2 -> GREEN 38/0). All GREEN logs include strict TypeScript, Biome and diff checks with exit0. These are local regression tests, not the 30-criterion qualification or OS-process recovery. Remaining kernel work includes full decision/effect decoding, explicit terminal decision status and receipt consumption, interrupted prepared-run recovery, derived classification and independent review. Workflow remains B.

Classification follow-up: a model can quote private purpose text into an adoption even with no evidence refs. Adoption visibility therefore conservatively inherits private purpose audience as well as every supplied evidence/adoption classification; fiction propagation includes supplied adoptions too. purpose-privacy RED 38/1 -> GREEN 39/0, strict types/Biome/diff exit0. This closes the reproduced purpose-canary leak; comprehensive receipt/domain propagation and source snapshot eligibility still need review.

Effect restore follow-up: receipt projection checks the owner effect ID and validates every persisted frame domain row/receipt before trusting its scope labels. decisionFrame uses the same decoder. effect-restore RED 39/2 -> GREEN 41/0, strict types/Biome/diff exit0. Complete decision/effect lifecycle decoding is still open. Independent reviewer child 01a08762-3525-7b61-ae87-7e1b4b96a14d reviews frozen commit 6ea612e; no verdict yet.

Decision restore now rejects unknown statuses and validates purpose/policy identity, method/expectation, deferred fields and adopted row shape at resume and wake reads (decision-restore RED 41/1 -> verified 42/0). Terminal request persistence now updates the corresponding decision status in the same SQLite transaction while preserving its frame and expectation; terminal-decision RED 42/1 -> GREEN 43/0. Unknown prepared-run recovery and the owner-result-saved/request-unfinished crash boundary remain open. No kernel completion assertion.

Saved-result recovery: pending effects include decisions whose receipt was saved but request finalization was interrupted. Resume validates and reuses the saved terminal receipt before querying an unavailable owner. Reproduction injects an exception after receipt persistence and resumes with owner reconciliation returning null and admission/model calls forbidden. saved-result RED 43/1 -> GREEN 44/0, stronger saved-owner-unavailable RED 43/1 -> GREEN 44/0; strict types/Biome/diff exit0. This proves in-process interruption boundaries only, not OS-process qualification.

Recovery validation follow-up: `effect-records.ts` validates all effect states, payloads, exact receipt identity and source-frame equality with the original decision; pending scans validate before filtering. A corrupted status cannot disappear (effect-status RED 44/1 -> GREEN 45/0). Terminal receipts reject contradictory replacement (receipt-replay 45/1 -> 46/0). Adoption eligibility now includes the entire actual source snapshot even if the proposal omits refs (omitted-dependency 46/1 -> 47/0). Effect snapshots must equal original decision snapshots (effect-source 47/1 -> 48/0).

Independent reviewer progress reproduced a late-unknown overwrite after concurrent successful reconciliation on frozen 6ea612e. Main reproduced it on current source and made run completion monotonic, returning the existing terminal trace (terminal-race 48/1 -> 49/0). A final-fence rejection now durably cancels the known-unadmitted effect marker instead of leaving perpetual unknown recovery (stale-marker 49/1 -> 50/0). All final logs include strict types/Biome/diff exit0. Reviewer final verdict still pending; full kernel remains B.

Final tool fence rechecks the current allowlist owner after pre-admit policy changes (tool-revocation RED 50/1 -> GREEN 51/0). Resume now reports unresolved judgment reservations even when no effect exists, and does not silently drop or retry the model call (interrupted-run RED 51/1 -> GREEN 52/0). These preserve uncertainty; an explicit cancellation/supersession protocol for interrupted model-only work remains a recovery concern, not automatic retry. Typecheck/Biome/diff exit0.

Kernel implementation/review is now closed: independent PASS at db65d2e and C receipt verifies 60/0 plus strict types/Biome/diff exit0. Earlier paragraphs are historical gap reports, not current open blockers. 015_kernel_result.md owns the final boundary and next direction; 014 maps deterministic contracts. OS-process recovery and model evaluation remain later registered work phases, not waived requirements.
