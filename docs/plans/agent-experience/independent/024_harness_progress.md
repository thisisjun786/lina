# Harness build progress, incomplete

A audit PASS covered 26a4717 plus 023_harness_audit.md, committed as 85c2911 before B. Reviewer closed. Main wrote shared harness-types.ts, strict public-case decoder, common serialize.ts and initial harness/environment tests. Receipt environment uses separate SQLite owner, immutable effect replay, actual lookup/arithmetic/submission/check feedback and unknown task results. No scenario-specific branches or evaluator truth are in these modules.

RED/GREEN evidence: public-case-red missing module -> green; serializer-red missing module -> two serialization cases green; environment-red missing owner -> environment-green actual incomplete submission/check failure and conflicting replay checks pass. Strict local typecheck now exits0. These are early module checks, not a runnable scored harness. Full multi-step parity, more input negatives, scenario exporter, mode sessions, runner, independent scorer and CLI remain to implement.

Executor child 01a0879f-c295-7430-b89e-6607753fdb5f owns only model.ts/model.test.ts; dispatch harness-transport-implementation attempt 295eab15-0928-482e-9f81-8815d5b7136d. Main has not accepted its result yet. No live model calls since initial transport smoke, and no scored trials.

Mode sessions and runner now execute baseline/kernel/ablation through one transport and owner interface, bridge actual tool/delivery receipts into subsequent inputs, apply public stage triggers and cap total model requests at six. RED missing modes/runner modules -> GREEN lookup-result-to-answer in all three modes, adoption inclusion across stages, invalid-output six-call limit, and unknown-submit new-purpose report with one admission. Initial raw-only parity verified; full corrected-history/receipt parity remains pending.

Transport executor completed model.ts/model.test.ts. Main inspected and requested fixes for stalled response-body timeout and null response classification; executor retained RED9/2 -> GREEN11/0 for those cases. Main inspected returned source and reran integrated checks: harness-progress-check.log 82 pass/0 fail, strict types/Biome/diff exit0. Transport evidence copied under transport-executor/. Executor reported complete and closed; main owns all files now. No live calls or scored results yet. Remaining: robust receipt provenance/alias parity, seeded scenarios and private truth, independent scorer/negative controls, subprocess CLI and qualification guard.

Independent scorer draft now has private truth types, explicit B01-B15 predicates, separate common quality/uptake and H/B aggregation. score-red missing module -> initial score-green; scorer-duplicate-red demonstrates repeated same episode could fill a row, fixed by rejecting duplicate episode IDs. scorer-duplicate-green.log includes 85 tests plus strict types/Biome/diff exit0. This is NOT a qualification-ready scorer: pending strict full trace/truth decoding; planned variant/subcase manifest validation (especially B15 both subcases); all independent negative controls; exact stage/receipt operand relationships and frozen structured-output handling; source-bound host evidence validation. Generator/exporter/CLI remain missing. No live scores or qualification claim.

Seeded B01-B15 scenario draft and separate public/private batch exporter added (64 episodes: four variants per row, B15 visible/omitted paired subcases). CLI export/run/score dynamically imports per subcommand; actual run path does not load truth/scorer. Initial generator/export/CLI RED missing modules -> GREEN. Frozen B10 requires a structured defer answer: a kernel deferred status alone incorrectly passed the first scorer draft, reproduced and removed (scorer-defer-red -> green). generator-cli-verified.log: 91 tests/types/Biome/diff exit0.

First real new-harness GLM development smoke: model label glm-5.3-flash, six HTTP successes but all proposals rejected due ambiguous action discriminator/string schema. Preserved full trace under external independent-kernel-evidence/harness-smoke/. Changed shared SYSTEM to explicitly name kind and show JSON-string answer encoding (no task answer supplied). Same development case then completed in one request, prompt595/completion439; separate CLI score process reports pass. Evidence external independent-kernel-evidence/harness-smoke-format-fix/{public.json,truth.json,kernel/trace.json,score.json}. This is a development B01 smoke, not held-out qualification, model ceiling or baseline comparison. No failed attempts discarded.

Remaining before harness completion: audit each generated scenario's exact frozen preconditions (B11/B12 prelude failed check and six-call feasibility), full paired-history parity, unique generated labels, strict trace/truth and manifest decoders, all scorer negative controls, compare/qualification CLI orchestration and source-bound host evidence checks, independent implementation review. Existing draft numeric aggregation must not be treated as qualified until these are complete.

Scenario contract repairs: B11/B12 now pre-execute the real submit+failed-check setup identically in all modes with host-generated stable per-episode prelude IDs. Earlier draft asked the model to create the first check, consuming an extra call and failing the frozen initial precondition. Distinct raw events now receive distinct source IDs; B15 previously reused a default source ID for 24 distractors. RED showed missing failed-check and duplicate identities; GREEN passes, plus actual input-cap test verifies required source visible/omitted in paired B15 inputs. Labels carry a monotonic suffix to avoid short-hash cycling collisions.

Scorer aggregation now uses seed/variant/subcase from private truth, rejects mixed/missing/duplicate batches, and requires both B15 subcases per each of four variants. A null kernel uptake cannot satisfy B11/B12/B13; affirmative evidence required regardless of compensating macro score. scorer-subcases and scorer-uptake RED/GREEN retained; latest scorer-uptake-green.log: 96 tests, strict types/Biome/diff exit0. HostEvidence aggregation tests use synthetic unit fixtures only, not actual host qualification proof. Full strict trace/manifest validation, complete negative controls and compare orchestration remain open.

File-boundary validation added: strict private truth fields/enums/variant/subcase and trace mode/transport usage/effect IDs/adoption/delivery references. CLI score decodes trace before scoring. Compare command executes each run and scorer in separate subprocesses, rotates mode order, checks case/truth file hashes, retains per-attempt output/exit/usage, and incrementally writes report. comparison-verified.log has 99 tests/types/Biome/diff exit0, including local ephemeral HTTP server and subprocess integration (sandbox requires escalation to bind local test socket).

Development live compare started with B03/B06/B11 variant0, three modes, seed development-compare-01. Output external independent-kernel-evidence/development-compare-01; current execution handle 39036. No final qualification claim. Six completed B03/B06 trials passed common quality; B11 still running when this entry was recorded. Preserve runtime source while job is active. A new compare-source-red test demonstrates started.json lacks source/model manifest fields; repair after job completion and do not retroactively claim the old run captured those fields.

Development compare-01 finished (9 trials, 27 requests): B03 and B06 common quality passed all three modes; B11 common quality failed all modes, while kernel uptake=true and ablation uptake=false as expected. Kernel hit six-call limit; baseline/ablation delivered truthful failure answers. Raw traces show repeated faulty method use after failed checks. This is evidence of working adoption transmission but no demonstrated task-quality improvement; no baseline gain or threshold claim. All attempts retained at external independent-kernel-evidence/development-compare-01/runs/report.json. Execution handle39036 completed and no task-owned server/process remains.

After that run completed, added comparison start source hash and explicit model/base URL/temperature/token/request limits (excluding API key); compare-source-red test failed before fix, compare-source-green full check passes99 tests/types/Biome/diff. Do not retroactively attribute the new source-manifest fields to development-compare-01. Next work: full correct-history parity and trace semantics, scorer negative controls and strict manifest mapping, then independent harness review. B11 failure should be analyzed without embedding scenario answers or weakening frozen predicates.

Manifest ingress now validates exact fields, unique episode and variant/subcase identities, content hashes, decoded public/private episode mapping, seed/row/variant/subcase consistency and final-stage reachability before subprocess dispatch. Qualification manifests require all64 planned subcases; development subsets remain explicitly permitted. manifest-red missing decoder -> manifest-green100 tests/types/Biome/diff exit0. Compare now uses this loader, so wrong row mapping is rejected before spending model calls. This still does not validate independent truth arithmetic or full scorer semantics; those remain review/negative-control work.

Mode boundary repairs: baseline raw history previously leaked a prior private source when later purpose switched public. baseline-scope RED -> GREEN now records subject/visibility separately and filters every actual baseline input. Explicit retraction events retain the source scope and remain in baseline history, while kernel excludes withdrawn source (baseline-retract RED -> mode-scope-verified102 tests/types/Biome/diff exit0). These are shared environment privacy restrictions, not baseline currentness filtering. Further multi-step receipt/answer parity remains pending.

Independent scorer review running on frozen17aa955, child01a087c1-cabc-7000-ab73-6849ae16d531, dispatch scorer-contract-review attempt9f3bc878-a822-4c0d-8b10-f81aadfe53e9; bounded to score/truth/trace/manifest/scenarios against005. Main owns modes fixes and will await concrete verdict before scorer changes. No final qualification claim.

Additional baseline scope fix: preserve all raw inputs internally so a later private purpose can see a source hidden during an earlier public purpose; every serialized input remains scoped (baseline-private-return RED/GREEN). Multi-step same-history parity now includes actual lookup receipt, answer delivery and a new purpose. First test exposed differing host UUIDs, so comparison-only stripHostMetadata normalizes structured effect/source/submission/verifier/fence references consistently; it does not alter model input or arbitrary output values. A negative verifies different tool values stay different. history-parity RED -> verified105 tests/types/Biome/diff exit0. Initial-only parity is no longer the sole evidence; correction/retraction relation and broader trace provenance still need full review.

Scorer reviewer FAIL accepted: B07 lookup outputs were disconnected from calculation operands; B09 accepted a self-created failure instead of checking the prelude submission; B13 could hide the original plan while admitting another stale-dependent plan. Common defect: existential predicates omitted identity/order/dependency joins. Preserve all three adversarial reproductions as rejecting regression fixtures before repairs; add positive controls and ask the same independent reviewer to recheck. No scoring threshold changes.

Scorer relations repaired after independent FAIL: original reviewer repros RED0/3, now reject all three and accept valid controls; tests also reject post-calculation lookups, cross-task repair and indirect stale plan ancestry. Added incomplete/limit rejection after separate RED. Whole check scorer-relations-green.log: 122 pass/0 fail, types/Biome/diff exit0 (existing lint infos remain). Ten runner-based always-defer/always-submit negative controls cover all three modes. Same reviewer is rechecking; no harness PASS or qualification claim yet.

Trace delivery integrity: ingress previously accepted a delivered answer whose bytes/audience disagreed with the actual owner receipt, because only effect-ID existence was checked. trace-delivery-red reproduces acceptance of forged text. Decoder now requires a completed @delivery receipt, matching original delivery args and delivered bytes/audience; four tampering controls plus real runner positive pass. trace-delivery-green.log: 123 pass/0 fail and strict types/Biome/diff exit0. Independent reviewer notified; review still pending.

Comparison drift guard: real HTTP test changed private truth during the first model request; compare-drift-red showed the old runner silently completed all modes. Comparison now revalidates source/manifest hashes and dataset hashes before each run and after run/score. On mismatch it records invalidated.json, clears qualification and preserves obtained trace/run logs. compare-drift-green: 124/0 full tests/types/Biome/diff; focused follow-up also verifies invalidation artifact and preserved response (2/0). This detects persistent drift at boundaries, not malicious change-and-restore races. No live qualification run.

Second scorer review FAIL on frozen c57c188 accepted: B11 omits answer-value comparison, B12 lacks method/condition truth and semantic uptake, B09 trusts fail label without exact missing-item receipt, delivery lacks answered-decision/stage join. Original B07/B09/B13 repairs confirmed closed. Repair all; B12 needs explicit structured condition contract compatible with frozen semantics, not arbitrary phrase matching. Reviewer repros preserved /tmp/b9a2-review/.

Round2 partial repairs: scorer-round2-red reproduces wrong B11 value, malformed B09 check and forged delivery decision; now reject each. B09 positive fixture now supplies actual check fields rather than the incomplete prior stub. Full scorer-round2-green: 128/0 tests/types/Biome/diff. Added explicit missing-adoption bridge fault test (9/0 focused) proving no second model call. B12 condition semantics remains OPEN: proposed generic structured method/condition encoding and matching private truth, sent to same reviewer for scope review; no final PASS or C transition.

B12 semantic repair: reviewer GO-WITH-FIXES accepted strict generic condition JSON {method,when}, applied equally B11/B12. Private truth now requires learnedRule, learningRequired and taskCondition (null/empty for other rows). Generator records original task values; shared SYSTEM documents encoding only. Uptake binds original prelude submit/check, exact one omission, rule, before-final adopted decision and same active adoption in final input. Task quality remains separate. Old private JSON lacks new required fields and must be regenerated, not silently rescored or rewritten. Actual runner negative RED2/0-pass -> GREEN4/0 tests includes both positive mode-task paths and bad condition/grounding controls. Original sparse B11 fixture now also lacks valid uptake; a full real-run positive is mutated to wrong answer to preserve independent value-regression coverage. Broader arbitrary natural-language conditional reasoning remains unproven.

Final learning-score-green.log: 132 pass/0 fail; strict types/Biome/diff exit0. 005 adds reviewed encoding clarification without changing rows/thresholds. Same reviewer rechecking round2 fixes; no C transition or qualification claim.

Isolation verification expanded: actual three-mode HTTP requests in separate runner/scorer comparison exclude a private-truth canary, private path, learningRequired and privateTruth fields. Private truth hash updated only in the synthetic test fixture before dispatch; no production dataset changed. Current literal-import graph from runner/model/public decoder reaches kernel/store/environment and excludes evaluator/generator/production Lina modules; nonliteral imports rejected by this audit. This is a source/transport boundary check, not OS filesystem isolation. private-canary-green focused2/0; isolation-green full133/0 tests/types/Biome/diff exit0. Independent scorer reviewer still pending; candidate changes in this pass are tests only.

Round3 reviewer FAIL accepted: B11 check can be failed-status/reordered and still pass; B12 private condition was not joined to actual serialized task evidence. Add real-runner adversarial mutations, enforce ordered completed check output and bind the same uptake request to actual derived rule and task payload. Earlier fixes independently confirmed closed; no metric change.

Round3 repaired: B11 requires completed submission followed by completed passing check with matching output ID and empty missing/extra. B11/B12 same final request joins actual serialized derived adoption and source-bound task payload method/condition/required; generator sources now identifies that later task. Actual-runner mutation RED -> GREEN. Full learning-relations-green.log 135/0 tests/types/Biome/diff. Stale-copy controls also now reach final B04/B13 stages yet fail common quality in all3 modes. Earlier stale-controls check overlapped edits while execution approval was pending, so it is not used as final candidate proof. Same reviewer rechecking. Repeated root cause is trusting summary fields without input/decision/receipt joins; remaining audit uses actual-chain mutations, not more shape-only fixtures.

Run registry implemented and wired: run-registry-red missing module -> real SQLite reopen, immutable start identity and terminal record checks GREEN. registry-integration-red shows comparison did not record attempts; compare now registers before subprocess calls in run-registry.sqlite beside output directory and records terminal execution status in a separate table. Same output cannot be re-registered; repeated seeds are retained as separate attempts so qualification can reject reuse rather than erase history. A killed process leaves started, not a guessed terminal result. Graceful errors record failed; completed means execution completed, not behavioral qualification pass. started.json records registry path. Full registry-integration-green.log:136/0 tests/types/Biome/diff. Pre-registry development runs remain archived but cannot be retroactively claimed as registry-backed qualification. Qualification validator/CLI still remains to implement.

Round4 independent FAIL accepted: serialized understanding could omit refs while identity fields matched grounded ledger adoption. Replace partial-field comparison with complete structural record equality, covering refs/status/sourceDecisionId and every other adoption field. Existing order and actual-condition fixes confirmed by reviewer.

Full serialized-adoption-green:137/0 tests/types/Biome/diff. Complete adoption structural equality closes missing-input-refs RED in both B11/B12. Registry qualificationCandidates now selects only last3 completed qualification attempts with same source, after freeze and unique seeds across all recorded attempts; qualification-history-red missing method -> GREEN2/0 registry tests. This selects candidate batches only, not passing scores; raw re-scoring/H proof/CLI remain pending.

Round5 independent FAIL accepted: original recorded adopt proposal could disagree with stored/serialized understanding. Add raw-response->parsed-proposal equality and adopted-decision/request->canonical adoption binding. Test both proposal-only mutation and matching raw response mutation; reject each before scoring.

Round5 GREEN: raw output equals recorded proposal; canonical adoption links to unique adopted decision/request and exact proposal fields plus supplied purpose revision. Real-run B11/B12 valid ingress passes, proposal-only and response+proposal mutations reject. Added rescoreBatch reading original manifest/truth/traces and run/score logs, checks source/config/model receipts and recomputes scores/usage while ignoring cached report/score booleans. rescore-red missing module -> real subprocess comparison GREEN, forged cached report has no effect and changed returned model rejects. Full adoption-proposal-green.log137/0 tests/types/Biome/diff; same reviewer rechecking; qualifier CLI/H proof binding still missing.

Round6 FAIL accepted: duplicate adoptions can reuse one adopted decision/request. Require deterministic adoption ID/revision, unique adoption IDs/source decisions and unique adopted request indices; retain real-run duplicates as regressions.

Round6 cardinality RED->GREEN includes deterministic adoption ID/revision, unique request/decision/adoption mappings. Host evidence validator added after missing-module RED: exactly H01-H15, source digest, exit0, artifact digest and row-specific Bun test output/command required; changed/missing/duplicate evidence rejects. Synthetic validator fixtures are explicitly not actual H passes. Full adoption-cardinality-green.log138/0 tests/types/Biome/diff. Independent reviewer rechecking; actual recovery proofs and full qualification command still pending.

Qualification command connected: `qualify --index INDEX_JSON --output NEW_REPORT_JSON` validates current source/generator/rubric hashes, registry last3 fresh complete candidates, original manifests and model config, H evidence and raw re-scoring; computes paired common behavior deltas separately from kernel uptake. Comparison now records generator/rubric digests and detects changes. Initial missing-module RED -> negative ingress GREEN; full positive all30/three-batch synthetic path and freeze-before-generation proof still UNVERIFIED/UNIMPLEMENTED respectively, so this command is not yet qualification-ready. No real pass claim.

Round7 reviewer duplicate cross-status decision FAIL accepted; global step decision-ID uniqueness now rejects it after actual-run RED. Full qualification-wired-green.log139/0 tests/types/Biome/diff. Same reviewer rechecking. Remaining gate work must establish generated-after-freeze records, complete synthetic positive/negative path and independent gate review before any qualification claim.
