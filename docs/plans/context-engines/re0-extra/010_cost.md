# Resource traversal cost

Depends on locked roadmap. MODIFY packages/lina-memory/src/resources/indexing.ts only if structural probe confirms repeated unrelated scanning. Current changed() loads one catalog then calls sources(collection,catalog) for every collection; sources filters and repeatedly scans the full catalog. Intended delta: build one transient ordered parent/collection-membership adjacency from the snapshot; sources visits only matching edges, retaining owner/visibility/version checks, cycle suppression, cap64, order and complete=false semantics. Snapshot lifetime ends with changed(); no persistent cache or schema.

NEW focused resource cost/graph equivalence tests under packages/lina-memory/test/ (exact name chosen after adjacent test search); no public export solely for testing. Instrument actual catalog/edge reads, compare reference traversal results on nested/cyclic/multiple membership/private/shared/deleted inputs. Independent roots at32/64/128 and larger bounded samples should not require one full catalog scan per root. Acceptance: linear snapshot construction plus visited edges for this empty-unrelated-root case; do not claim all overlapping collections can be globally linear. Report wall time separately without invented SLO.

Drive an isolated actual ResourceStore mutation through HTTP fleet routes with synthetic provider functions to ensure changed job IDs and parent dispatch remain correct. Search pagination cache reuse and image-feed batch reads reuse prior proof where source unchanged; add repeated-cycle resource retention observation to distinguish growth from bounded cache. No assertion or timeout weakening. Source-of-truth update: parent092 evidence and this cycle result. Independent plan review before implementation; bounded indexing executor may write only indexing.ts and its named new test while main owns HTTP/performance evidence.

Audit amendments: test host is MODIFY packages/lina-memory/test/resources-indexing.test.ts. Build adjacency over the unfiltered snapshot, preserving snapshot order in each edge list; apply root-specific owner/deletion/shared-version predicates only during traversal. Include >64 reachable members through multiple parents/memberships, comparing exact sorted refs, sourceDigest and complete=false to the original reference algorithm. Cap-boundary order is durable job identity, not cosmetic ordering.

Measurement precision: prior cost.ts counts catalog.filter element reads, not the outer changed loop or the filtered-array BFS comparisons. Report it only as that metric. Add a second counter for candidate relationship checks (parentId/collectionIds accesses on traced resource objects) and an independent reference traversal oracle. No claim that the old metric measured all work. SQL prepare spies and a narrow typed test-only view of the existing private sources seam are allowed; no product export solely for testing. Count snapshot construction separately if instrumented; otherwise label the one-pass construction bound as source-derived.

Read-path scope: list/valid/read can rebuild collection snapshots independently. Measure a representative collection read/search too and record residual amplification; do not claim changed()-only adjacency fixes all search costs. Extend scope only with a concrete measured counterexample and amended plan. Gate is elimination of repeated unrelated collection scans during one mutation, plus output identity equivalence, not an ungrounded end-to-end linearity promise.
