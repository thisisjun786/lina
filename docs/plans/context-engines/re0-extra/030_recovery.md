# Combined cancellation and recovery

Depends on both engine cycles. NEW isolated diagnostic probes under task scratch using actual FleetResources/ResourceEngine/resourceRoutes and separate Bun processes, adapting prior restart-server.ts. Signal at model entry; issue cancellation/revocation at owned boundaries; kill only owned writer, reopen same isolated SQLite in a new process, exercise explicit retry and operation replay. Provider functions are synthetic and counted; no external endpoint.

Activation cases: acknowledged pending work, prepared uncertain work, duplicate operation before/after crash, authority revoked before retry and between prepare/commit. Observable gates: pending explicit recovery reaches ready once; uncertain claim is not auto-reexecuted; stale/private output is withheld; original receipt replay does not create a new semantic effect; independent authorized work still succeeds. Preserve raw HTTP/process/DB receipts and distinguish explicit retry from automatic startup recovery.

Conditional product delta is restricted to a reproduced failing boundary in packages/lina-memory/src/resources or runtime resource dispatch, with a concrete per-file amendment and regression before editing. Do not preemptively add retries or recovery services. Verification-only NOOP is valid if all new scenarios satisfy contracts. Final source applicability check, independent audit and required current hosted gate; existing first-attempt CI failures stay in the evidence record. Update parent092 and cycle memo with what did not improve.

Audit amendment: prior harness is the absolute task artifact /home/jun/tmp/lina-pr7-re0-critical-01a08577/restart-server.ts, verified by main in this environment. It imports actual FleetResources from packages/lina-runtime/src/fleet/resource-runtime.ts and resourceRoutes from fleet/resource-routes.ts; it is not a tracked repository file. If unavailable in a delegated environment, use its source supplied by main or build the named task-owned probe directly from these production entrypoints; do not substitute retired OpenViking scripts.

Discovered boundary for this cycle: real HTTP cap-http.ts links63 selected children then the highest-ID64th child to the same collection (root counts as one). Current source traversal changes complete=true tofalse while refs/digest/job ID stay identical; API output remains complete=true, stale=false. This is confirmed false completion, not an optimization metric. Preserve cap-http.json/cap-summary.json as RED corpus. At this cycle P specify the minimal stored/read completeness reconciliation with backward compatibility, exact64/65 and removal/reopen tests; no automatic provider rerun or wholesale job-key migration without evidence. Keep this outstanding until actualHTTP and restarted storage no longer overstate completeness.

Revalidated wp3 P after wp2 D4c99896: indexing remains at verified656ae32. Confirmed cap counterexample and separate-process legacyDB are preserved. Exact proposed MODIFY indexing.ts: changed() reconciles existing same-key job.complete with current source.complete, preserving token/state/attempt/outputHash and reporting the resource as affected; list() computes sources once and projects current complete for same-digest jobs (covers old untouched databases); read() verifies original output hash first, then returns output.complete AND current job projection.complete. prepare/retry capture current source completeness when writing their existing job; complete() gates submitted result with current source coverage and records that coverage alongside the normal ready result. No job-key, schema, attempt-budget, output-receipt migration, or automatic model rerun. Do not infer true from a legacy output that was already false.

NEW packages/lina-memory/test/resources-completeness.test.ts: actualStore63children+root complete; attachlexicallylast64th child without changing selectedrefs -> samejob but incomplete; detachextra -> previouslycomplete output becomescomplete again without reexecution; explicit originallypartial output remainspartial; close/reopen and existingoldDB reads; activepreparedclaim changedcoverage commitsincomplete. Normal64/65 and parentdispatch affectedIDs checked. NEW/extend isolated runtime capHTTP probe with oldcursor invalidation and newprocessread. Add a current-state search control showing complete changed even when resource revision/ref unchanged.

Combined process probe NEW task scratch: actualresourceRoutes/FleetResources server with signal-held synthetic summarize callback; create sharedsource and acknowledgedpendingwork; revoke sharedsource toprivate, assertotherowner denial, SIGKILLownedwriter afterprepare, newreader sameDB makesnoautomaticcalls; explicitretry asotherowner denied, authorizedpendingretry succeeds exactlyonce, operationreplay/duplicate retry doesnot duplicate effects, olduncertain/staleclaimcannotpublish. All roots/ports/process IDs task-owned; preserve raw requests/outcomes. Existing full-fleet authority/native/image proof reused only for identical sources; supplement image-query growth measurement if prior constant-scan proof does not cover larger persisted history.

Execution evidence: coverage-red.log had4failures before the repair; coverage-tests.log now6pass32assertions including raw receipt tamper rejection and legacy pending prepare/retry attempt preservation. The unmodified pre-fix cap-state database now reads incomplete through a new Bun process with0calls. cap-cursor-http.ts confirms actualHTTP overflow incomplete, oldcursor409, removalrestores originallycomplete output, root summary calls64before/after (no new parent generation). Original partial output stayspartial.

Combined process runs: shared->private while first synthetic summary is held, thenSIGKILLownedwriter; newreader performs0automaticcalls, foreignprivate retry/read404, ownerrecovers2pendingresources with2summarycalls, operationreplay0extra, terminalretry400, obsoletejobretry409, oldtokencompletion409, sharedpositivecontrol200. A separate SIGTERM cancellation run produces cancelled/failed instead ofunknown, exits0, and the same authorized recovery/denial/replay checks pass. All owned application processes stopped; task-only DBs/logs retained. These are actual HTTP and distinct OS processes with synthetic service functions and actor mapping, not external-provider or production-login proof.

Image-history supplement:16/32/64/128 real WorldStore prepared attempts produce281/553/1097/2185 SQLprepare calls and roughly10/20/36/76ms local read time; serialized size grows roughlylinearly. Predetermined query-growth gatepassed, no externalgeneration. Prior unchanged fullFleet/web image-byte/revocation and one-attempt-snapshot-per-feed-request proof remain separate from this history measurement. No universal latency or asset-byte storage SLO is inferred.

Final bounded implementation review: Auditor PASS, no blockers; job identity, token/attempt accounting, authority checks and raw output hashes preserved. Main roottypecheck/lint/ci:build exit0; final full suite and hosted candidate qualification remain in the task report. Unchanged image fullFleet/web proxy/restart/revocation probe also passed18assertions, oneattemptsnapshot andoneassetread forone requestedpost amongnine.
