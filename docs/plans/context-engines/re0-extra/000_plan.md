# Additional engine quality cycles

The prior critical sweep repaired known faults and passed local and hosted gates, while leaving collection scan growth, conservative mixed reconfirmation rejection and combined cancellation/recovery as explicit limits. This unit tests those boundaries and repairs measured defects without weakening normal behavior.

Archetype: satisfy-spec with measured cost hypotheses. Trigger: Jun explicitly requested three additional re0 cycles in this same task. Goal: bounded unnecessary work, equivalent reasoning batch outcomes and durable recovery without duplicate effects or stale authority. Non-goals: dialogue quality, features, merge/deploy, external paid product providers, installed user data. Scope: memory/resource engines and their runtime integration/tests; existing PR only.

Stop: each of the three cycles has actual surface proof, normal controls, negative corpus and independent review; confirmed material defects are closed and final relevant gates pass. Failure/blocked is reported with evidence, never converted into a success by timeouts/retries/skips. No user token or wall-clock budget was specified; no external product-call budget is authorized. Test/process lifetimes are bounded by existing tests or explicitly owned signal-driven probes.

Verifier: Bun test targeted existing memory/resource/runtime tests (direct path arguments), root typecheck/lint and hosted current candidate when code changes. Prior local full3528/47skip/0fail and hosted attempt2 green apply only to unchanged sources/criteria. Roadmap-only verification checks file existence and checks whitespace; no application-code correctness claim from docs checks.

Order: wp0 locks the criteria and roadmap; wp1 resource traversal cost; wp2 reasoning semantics; wp3 combined interruption/recovery on the resulting engine. This follows verified subsystem contracts into final integration, not effort ranking. Each consumes the previous recorded result and revalidates its exact diff before editing. re0 provides the build/drive/memo discipline; one main-owned persisted phase ledger tracks it, with no parallel workflow.

Artifacts: this numbered unit, cumulative parent092 evidence, isolated scratch directory /home/jun/tmp/lina-pr7-re0-extra-01a08577. Expected outcomes: DONE for proven criteria, NOOP for unchanged behavior with new evidence, unresolved findings remain open. Main owns reviewer judgments and reclaims failed delegated slices; user decision only for genuinely new external/destructive authority. No permission re-request for approved fixes/commits/push/CI.

New schema fields: none planned. No protection bypass or new enforcement layer is proposed; tests supply evidence, not universal runtime guarantees.

Audit synthesis: four planned docs, not five (the dispatch follow-up had a counting typo). Accepted stronger cost instrumentation, cap/digest order controls, explicit read-path limitation, two-stage reasoning validation, generation invariance and support-change rejection. Rebutted missing-evidence claims: the harness and final CI logs are task artifacts outside Git, verified by main at /home/jun/tmp/lina-pr7-re0-critical-01a08577/{restart-server.ts,final-local-full.log,hosted-attempt2-tests.log,REPORT.md}. Hosted run34353905184 attempt2 on5e656a81c5921bcced9099f91b7b7ac90c494df0 has3528pass/47skip/0fail; these are not the old parent092 baseline counts. No claim that the reviewer independently accessed those artifacts. This cycle does not recertify those old results; it pins their provenance and applies them only to unchanged criteria.
