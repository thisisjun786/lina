# Preserve safe mixed reconfirmation

Depends on resource cost result recorded. MODIFY packages/lina-memory/src/engine/reasoning.ts and store.ts only after regression reproduces safe mixed reconfirmation rejection. Current prepareConclusions rejects any transitive target; applyConclusions already distinguishes equivalent conclusion content, premises and proof/support changes. Intended delta: share the exact non-invalidating equivalence decision, validate the full prepared batch against pre-write state, and reject dependency-crossing changes while allowing only demonstrably non-invalidating reconfirmation. Do not create two subtly different equivalence definitions. Preserve source fences, eligibility and cycle checks, direct-observation protection and transaction rollback. No schema migration or retrospective repair.

MODIFY packages/lina-memory/test/engine-reasoning.test.ts with both output orders of identical ancestor+new descendant, changed value/premise/kind controls, proof/support corroboration and source revocation. Must fail old conservative code for safe case and retain old harmful-batch negatives; assert actual recall and reopen, not committed count only. Run actual CompanionMemory synthetic-model flow for a reachable prompt-page batch, observing bounded calls, persisted job state and visible outputs. Main handles this tightly coupled change locally; independent reviewer gets complete diff and original counterexample.

If the exact equivalence mechanism cannot be safely shared, retain guard and report criterion unresolved, not success. Update parent092 and cycle result with proven limits.

Audit amendments: use two stages before writes: prepare all proposals with existing eligibility/cycle checks while collecting cross-target ancestry, then classify each prepared target against current store state and validate every cross-target dependency before applying any record. Keep store eligibility, sourceInvalidated and slot-fence checks in the store; do not import persistence into pure reasoning helpers. Existing direct and transitive rejection remains the default unless the store supplies verified admission; alternative exact API shape is revalidated at this cycle's P.

The existing sameConclusion predicate at store.ts539 preserves generation by matching sameValue, reasoning kind and sorted(recordId,contentHash); the stricter store.ts559 branch skips all writes when sources/proofs/support also match. They serve different purposes and must not be conflated. Share generation-preserving identity with the write loop, but admit a mixed dependency only when its ancestor contentHash AND support remain unchanged. Support-changing mixed batches remain rejected until dependency support propagation is independently established. This does not claim pre-existing single-record support propagation is correct; probe reachability before classifying a defect.

Assertions compare semantic persisted state across both orders: revision, generation, premise IDs/content hashes, support, status and recall after reopen. Normalize independently generated fixture IDs/request metadata only; do not ignore behavioral fields. Harmful cases throw the same pre-write validation error and preserve all prior state. Proof-only corroboration must retain generation and support for admission; source fences and revocation always win. A generation bump or support mismatch cannot use the relaxed path.
