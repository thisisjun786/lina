# 050 — Real work, shared persona and memory boundaries

Status: implementation and independent repair review PASS, 2026-09-08; final C source-bound evidence is recorded below. Source baseline `3ff96e3`. Depends on completed [010](010_state_and_views.md) and [040](040_autonomous_life.md). The prior D direction is: “050 real-work receipts and ordinary shared persona/memory … actual task receipts, one-time work influence and source-aware ordinary persona/memory integration.” This unit follows that direction. 060–080 remain required.

## Unit contract

| Field | Scope |
| --- | --- |
| Loop / trigger | Satisfy-spec; continue the user-authorized unattended LIFE implementation. C4 for provenance, migration, disclosure and native-context boundaries. |
| Goal | Actual task outcomes influence later LIFE choices once; ordinary Lina and NPCs use the same permitted growth while fictional material remains distinguishable from factual conversation memory. |
| Non-goals | NPC authority over real tasks, model-authored success verification, guessed sharing/cadence/budget defaults, live provider or Honcho writes, installed-user-data changes, external SNS, image/UI implementation, push/merge/deploy. |
| Verifier | Actual TaskManager RPC notification/reconcile, real source/world SQLite and process restart, actual serialized ordinary Codex requests with native memory enabled, real Companion/Engine/Context/Honcho local fixtures and negative source-policy tests; source checks and manual management HTTP. |
| Stop / outcome | Close only after the field chains and acceptance rows below are implemented and independently reviewed. PASS describes local source/transport behavior, not real model expressive quality. Continue060; no incomplete migration or disabled native memory shortcut counts as done. |
| Artifact | This prewritten decade document, task-owned `work-memory-*` evidence and the existing goalplan ledger. |
| Delegation / escalation | Main owns shared provenance, native/runtime admission, world integration and persona. B workers have the disjoint scopes below after contracts freeze. Main reclaims after two distinct failed packets. Additional delegation needs a recorded plan amendment. Missing external authority leaves that qualification unperformed, without blocking authorized local implementation. |
| Resources | No user token or elapsed-time budget. Tests use bounded synthetic data, temporary roots and local Responses/Honcho servers. No configured paid inference or installed state. |

## Revalidated source baseline

The040 result is `3ff96e3`:1,890 passing tests/9,277 assertions with both native flags, root/browser types, lint, build/CLI and document checks. This is baseline proof, not050 behavior. The root test pattern and TypeScript includes cover the planned package files; planned new tests below do not exist yet.

`TaskStore` schema is1 (`lina-codex/src/task-schema.ts:4`). `completeTurn` is also called when a turn starts, so its name or an `idle` state cannot establish terminal success. `tasks.ts:619` currently maps unknown completion status to idle; `reconcile` calls `applyNative`, whose early unchanged-state return can miss additional historical turns. Both paths must create receipts from explicitly observed terminal turns. `handover` currently stores only the latest owner, requiring an append-only attribution record.

`DurableStore` schema is1 (`lina-core/src/store.ts:15`); entries contain role/text/raw, and only the user entry is linked by `requests.entry_id`. `PromptLane` holds the Lina request ID but `CodexContextPolicy` currently generates a separate request ID for transport. `PromptAdmission` must carry the trusted correlation ID; assistant/tool associations cannot depend on text matching or model-supplied raw JSON. `CodexContextPolicy` already persists exposure intent, outcome and native epoch. Reuse those receipts and012 scope transitions rather than creating a second native-context history.

Memory source investigation found Engine schema2, Companion queue3, Context schema1 and Honcho outbox1 (Honcho HTTP `/v3` is unrelated). Companion jobs and Engine request IDs are user-entry IDs; they are not the Lina request ID. Existing record/receipt shortcuts, KNOWN SLOTS, summary ancestry, working context, preference/reflection seeds and extra history searches bypass a scan-only filter. `work-memory-p-source-map.json` records the inspected owners and the read-only investigator's limitations.

`installPersona` uses empty dynamics in native mode, and its growth tool currently returns native state or agent changes, including causes. `AgentStore` changes carry source-entry IDs; `ConversationStore` preferences also carry source-entry/request IDs. Their stored learned values need qualified model views, in addition to filtering new reflection. Authored profiles and explicitly configured conversation style remain intact.

`startPersistentApp` still rejects `world` before initialization. Keep that guard until the source-aware composition passes integration. `SessionContextPolicy` v1 conversation currently forbids disclosed LIFE and `installWorldContext` registers world-read only for LIFE. Explicit ordinary recall therefore requires a named new policy contract and current recipient permission, not quietly widening the old v1 rule.


## Changes and field path

| Operation / exact path | Before → after |
| --- | --- |
| MODIFY `packages/lina-codex/src/task-types.ts`, `task-schema.ts`, `task-store.ts`, `tasks.ts` | Turn completion updates status/notice → transaction also writes durable work receipt/outbox, queryable until acknowledged |
| NEW `packages/lina-runtime/src/life/work-bridge.ts` | No source integration → typed receipt subscription/poll, owner check, sharing policy, idempotent world inbox |
| MODIFY `packages/lina-runtime/src/fleet/codex-fleet.ts`, `fleet/task-routes.ts` | Wire task service receipts and explicit evidence/consent changes; no arbitrary model-authored success events |
| MODIFY `packages/lina-core/src/agents/persona.ts`, runtime `persona/hooks.ts`, `session-app.ts`, `fleet/manager.ts` | Existing dynamics/native preferences only → bounded `SharedPersonaView` alongside authored identity and real memory, refreshed at each turn |
| MODIFY core `protocol.ts`, `entries.ts`, `requests.ts`, `store.ts`; NEW core `source-policy.ts`; MODIFY Codex `session.ts`, runtime `session-app.ts`, `world.ts` | Trusted request/entry provenance created at the owning runtime boundary, journaled before settlement and restored with strict schema migration |
| MODIFY runtime `context/companion.ts`, `companion-queue.ts`, `capture-scan.ts`, `memory.ts`, `native.ts`, `persona/native-preferences.ts`, `persona/reflection.ts` | Gate actual native/Honcho collection, preference extraction, pending jobs, compaction and recall using journal provenance |
| MODIFY runtime `context/external.ts`, `coordinator.ts`, `tools.ts`, `memory-query.ts`; core `context/store.ts`, `context/schema.ts`, `entries.ts` | Preserve source-policy unions in LCM/working context and filter model-facing archive/search/expand paths; UI history remains independently available to its authorized viewer |
| MODIFY `packages/lina-memory/src/engine/types.ts`, `prompt.ts`, `validation.ts`, `store.ts`, `schema.ts`, `audit.ts`, `records.ts` | Source role/text only → trusted source policy lookup, persisted eligibility/provenance version, derived-record audit and query filtering |
| MODIFY memory `honcho/types.ts`, `outbox-schema.ts`, `outbox.ts`, `capture.ts`, `client.ts`, `messages.ts` | Retain source policy across chunking/outbox/retry and query scope; prevent legacy uncertain data from reentering normal context |
| NEW core `test/life-persona.test.ts`; Codex `test/task-work-receipts.test.ts`; runtime `test/life-work.test.ts`, `life-persona.test.ts`; MODIFY Codex `test/world-session.test.ts` | Task-store through world effect through real serialized next-turn request, restart and privacy negatives |
| NEW core `test/source-policy.test.ts`; memory `test/life-source-policy.test.ts`; runtime `test/life-memory.test.ts` | Actual Companion queue and Honcho capture/recall paths, legacy pending work and forged-source negatives |

New `WorkReceipt` fields are created only by the trusted task owner using stored task/turn revisions or explicit evidence records; serialized in a new `task_work_receipts` table within task-store transactions; strictly decoded by a typed TaskManager API; consumed by `work-bridge.ts`, then world input/director. Preserve `sourceTaskId`, `sourceTurnId`, receipt ID/revision, managing agent, verified participant IDs, outcome category, evidence references and permitted summary fields. Raw cwd/prompt/logs are not exported to LIFE.

## Contract diff

```ts
// Before: turn/completed -> status "idle" | "failed" | "interrupted" + notice.
// After: retain these task states; a separate receipt does not redefine idle.
interface WorkReceipt {
  version: 1;
  id: string;
  taskId: string;
  turnId: string;
  taskRevision: number;
  receiptRevision: number;
  ownerAgentId: string | null;
  participantAgentIds: string[];
  attributionStatus: "known" | "unknown";
  outcome: "turn_ended" | "verified_result" | "failed" | "interrupted";
  supersedesRevision: number | null;
  correction: null | { kind: "amend" | "retract"; reason: string };
  evidenceRefs: WorkEvidenceRef[];
}
// Task transaction -> source outbox -> world inbox -> source ack.
// One key identifies one receipt revision; replay never reapplies growth.
```

`turn/completed` currently maps a non-failed turn to `idle`; it is not task success. `verified_result` requires an explicit linked verifier/user confirmation record, including verifier identity and immutable evidence reference. A model claiming “tests passed” is reported evidence until independently established. Participant attribution needs actual task participation/handover records; do not infer collaboration from a mention. Unknown task statuses remain inspectable and cannot become success through a default branch.

Receipt creation must also cover terminal state discovered by `reconcile()`, not only live RPC notifications. Recovered completion uses the same task/turn identity. A source outbox in `tasks.sqlite` and an inbox in the world DB allow safe crash/retry without pretending both DBs share a transaction. Update strict task schema migration/version validation; reject unknown schemas and preserve old pending approvals/notices. Cursor-only polling of current task state is insufficient because it loses intermediate completions and corrections.

Source corrections/retractions create a new receipt referring to the old one. They revise future eligibility and add correction experiences; past fictional events remain history. Revoking share permission blocks future exports and withdraws applicable derived publications, without claiming to erase already viewed/downloaded material. Configure what task categories/fields may influence LIFE; absent sharing choices export no task content.

Persona composition preserves the complete authored identity and explicitly distinguishes adaptable traits from anchors. Both ordinary chat and LIFE read the same committed growth revision. Existing native memory mode must not suppress LIFE growth accidentally: `nativeDynamics` currently substitutes `emptyDynamics()`, so growth is a separate option, not hidden in that replaced object. `lina_persona_read(section: "growth")` returns only the safe projection; it must not expose internal growth cause records.

Change the prompt authority contract deliberately: the current `CORE_AUTHORITY` says learned data cannot change the core, and dynamics are used only gently. Keep the authored source intact, but compile stable identity/permissions/explicit user locks separately from initial adaptable temperament. Validated current traits/attitudes govern those adaptable dimensions, rather than becoming decorative background overridden by the initial personality sentence. Expose the precedence in `composePersonaPrompt` and use the identical rule in actor input. If an axis conflicts with an explicit authored lock, reject that growth; no free-form model rewrite of the source profile. Tests must show an allowed behavioral contrast as well as preserved anchors, not just presence of a new JSON field.

Explicit event recall first applies the caller's disclosure policy and labels returned content fictional. Ordinary memory capture cannot turn a fictional event, feed post or generated image into an observation about the user. Keep simulation records in the world store; if a memory adapter later indexes them, require a separate namespace/source type and lossless provenance. No generic append to native user observations. Compaction and resumed-thread context must retain the boundary.

## Memory source contract and migration

The actual native capture path is `CompanionMemory.scan()/process()` in `context/companion.ts`; the Honcho path is `capture-scan.ts` → `HonchoOutbox` → `CaptureDelivery`. Both presently select settled user/assistant text. `buildObservationPrompt()` currently reduces sources to entry ID/role/text, and `validateSources()` checks roles/quotes without a fiction boundary. A clean native thread alone cannot prevent the retained journal from being scanned again.

| Stage | Required field path |
| --- | --- |
| Create | Runtime resolves request purpose, current authorization scope and actual or possibly delivered LIFE/tool/material references, including still-retained exposure from prior turns in the same native epoch. Journal owner records `SourcePolicy {version, scope, sessionId, requestId, nativeEpoch, scopeDigest, policyRevision, contextReceiptIds, materialKinds}` with scope `ordinary`, `life`, `mixed` or `unclassified_legacy`. Actor/SNS input is LIFE; an ordinary turn with retained disclosed event/secret material is mixed even without a new recall. Growth-only context is explicitly distinguished and does not itself taint a factual user message. Neither user/model JSON nor imported transcript `raw` can grant ordinary eligibility. |
| Persist | Core journal adds recognized request-source and entry-source policy tables; request context receipts and entry associations are durable before settlement/observer admission. Tool disclosure arriving mid-turn updates that request's policy before it can settle. Append/import APIs must provide trusted origin or receive unclassified status. Derived summaries retain the union of source policies. |
| Restore | Strict journal/queue/engine/outbox migrations preserve source IDs, historical failures and receipts. Decode policy versions with trusted lookup; absent/unknown/conflicting provenance is unclassified, never inferred ordinary from role. Old native headers/journals remain viewable. |
| Consume | Companion scan **and** process-time retry, native preference extraction, reflection, Honcho enqueue **and** dispatch, compaction, EngineStore apply/state/recall, known-slot prompts and ordinary runtime recall all check the same source policy. Models receive the permitted projection; final validators repeat trusted lookup and reject ineligible sources even if a model cites a valid quote. |

For first-party LIFE/mixed episodes, exclude the affected episode from automatic factual/native growth extraction; do not ask a model to strip fiction and then trust its label. This can withhold genuine preferences in a mixed episode: record that limitation and allow later independent ordinary user evidence rather than laundering the whole episode. Native self/relationship learning from actual conversation remains supported, but LIFE-derived growth cannot be duplicated into that independently editable engine. A purely ordinary user claim is still a claim, not independently verified physical truth; this policy does not solve all semantic fiction detection in arbitrary unmarked text.

Before processing old pending/running/failed jobs, re-resolve their source eligibility; preserve retry/error history and mark ineligible work withheld with a reason. Do not clear queues, count withheld work as successfully learned, or let one legacy entry fence all new eligible episodes. Existing memory records are audited against source lookup: ineligible/mixed/unknown-derived records are excluded from state, known slots and recall, with original history retained for inspection/correction. Rebuild from eligible sources into a new audited revision only when the source chain is provable. Eligibility changes invalidate recall/summary caches and stale observer results before apply.

Honcho chunks carry trusted source policy IDs/digests and a policy-versioned capture scope through the outbox. Recheck on delivery, including pending/unknown retry after restart. Already uploaded uncertain legacy content must not remain silently queryable as ordinary memory: if remote source-level filtering and proof cannot be established, withhold its recall and use a separately identified ordinary-only namespace after validation. Keep old remote data and disclose the recall limitation; no destructive remote cleanup is implied. `MemoryBridge.recall()` must reject unqualified scope/results and clear cached text, not accept arbitrary `result.text` merely because the service returned success. Remote API capability and migration cost are verified before enabling that path.

## Acceptance scenarios

| Trigger | Observable result |
| --- | --- |
| Drive actual TaskManager `turn/completed` using fake RPC, then source/outbox/inbox APIs | Receipt exists durably; world opportunity changes using receipt, not a manually fabricated input |
| Completion learned during reconnect; repeat notice and outbox delivery | One receipt per source revision, one causal LIFE input; no lost terminal turn |
| Ended turn without verifier, failed task, unknown status, forged owner/evidence | No `verified_result`; invalid authority rejected; completion remains distinguishable |
| Crash after task commit / after world inbox commit / before source ack | Retry delivers once; evidence and task approval state survive migration |
| Correct/retract prior evidence or revoke sharing | Future candidate conditions update; explicit correction history, blocked publication; no rewritten old event |
| Commit a permitted habit/attitude, ordinary chat with native memory enabled | Exact next `turn/start` includes new shared fields; authored name/voice/anchors unchanged |
| Insert secret into growth rationale, persona-read result, compaction input, explicit recall without grant | Capture all serialized model/tool inputs: secret and private identifiers absent |
| Subsequent factual user-memory extraction after LIFE exposure | No fictional event/image promoted to supported user fact |
| Settle an explicit LIFE recall plus user reply, then run real Companion scan/process and restart | Episode policy survives; no factual/native-growth/preference update from the mixed episode; unrelated ordinary evidence still learns |
| Feed a valid fictional quote directly to EngineStore validation or forge an ordinary policy in user/model `raw` | Trusted source lookup rejects it; changing source metadata in content cannot grant eligibility |
| Existing pending/failed Companion jobs or Honcho chunks contain old LIFE text | Preserved queue history and visible withholding, zero observer/capture dispatch for ineligible work; new eligible entries progress |
| Existing contaminated engine record/known-slot/cache or unqualified remote recall | Absent from next memory prompt/tool response/ordinary turn; original evidence retained, no false successful migration |
| Old LIFE text exists in external summary, working context, `lina_history_search` or `lina_context_expand` result | Actual source-aware summary/query path excludes it before ranking/budget/output; preserved UI journal does not grant model access |
| Crash after context receipt, before entry association or settlement; downgrade eligibility while observer runs | Recover associations consistently; unclassified source cannot be admitted; stale result rejected at apply |
| A prior turn disclosed LIFE, then a turn has no new world tool, then permissions are revoked | Native exposure lineage still governs the second episode; revoked scope forces 012 context transition; old mixed episodes never become ordinary by relabeling |

These scenarios now have implementation tests; the checkpoints below distinguish passing components from final unit acceptance. Existing task/persona regressions also run after changing their owners. The first-unit [012 activation contract](012_context_migration.md) requires this phase's source-aware service composition and coordinated identity-policy edits before installed sharing is enabled; disabling the memory backend alone does not satisfy it. A passing fixed-response request test proves delivery, not expressive quality; 080 checks actual later replies. Store/API validation is the application boundary (E7); source proof can still be wrong if the configured verifier lies, and privileged source editors can bypass it. Report that limitation, do not call an unverified summary proof. SoT: `docs/CODEX_RUNTIME.md`, `docs/VALIDATION.md` and planning index.

## Concrete 050 contracts for A review

### Trusted journal source and native correlation

Core `source-policy.ts` owns strict `SourcePolicy` parsing, digesting and union rules, `SourceProof {entryId, policyRevision, policyDigest}`, and `SourceLookup(entryId)` returning the entry with its current trusted policy. The policy includes version1, scope (`ordinary|life|mixed|unclassified_legacy`), binding session ID, request ID, native epoch, native scope digest, policy revision and exact context-receipt IDs/material kinds. Content text/raw and imported transcript fields never create ordinary eligibility. Every consumer uses this resolver; a stored eligible flag is not independent authority.

Journal schema1→2 adds request policy history/current references, entry-to-request/source associations and transport-exposure references. Existing entries lacking reconstructible trusted provenance remain unclassified, viewable but ineligible for factual extraction. New admission registers the request before native dispatch; source/entry associations commit before settlement. Policy may only gain restrictions or retained evidence after admission. A historical legacy episode cannot be relabeled ordinary using a caller-provided claim. Corrupt versions, orphan associations or altered receipts reject at startup before observer dispatch; actual v1 bytes and interrupted requests survive migration.

Add `PromptAdmission.requestId` as an optional compatibility field, supplied by `PromptLane` for all managed prompts/steers. The adapter uses it in `context_run` and emission provenance. Direct adapter clients without managed provenance remain unclassified to memory. Add trusted native source/exposure callbacks to `SdkSessionOptions` and a source lookup capability to `SessionPort`; `sdk-events.ts`/`runtime.ts` must not mistake a user/model JSON value for one of these transport events. Journal and JSONL recovery compare stable entry IDs, native epoch and request bindings. A partial crash with missing associations is withheld until the exact owned native proof is recovered; it is never assumed ordinary.

Scope is episode-based. At request start include all material still retained from earlier turns in that native epoch; during a tool call record planned exposure before returning bytes, update that request's policy and all its associated entries before it settles. Planned/unknown delivery counts as exposure. Do not retroactively taint an earlier settled ordinary request merely because a later request disclosed LIFE in the same epoch. A later request with no new recall still inherits prior exposure. Growth-only material is ordinary-eligible; LIFE purpose is life, and ordinary purpose with disclosed LIFE is mixed. Revocation uses012 to rotate native context; old mixed episodes stay mixed.

All ordinary sessions use the source-aware native composition, including when no world is selected. Older unclassified native history is not copied into a fresh ordinary native context as a compatibility fallback. UI history stays available. Model-facing status/context tools also use qualified views; `createStatusTool` cannot expose unfiltered working state or active summaries.

### Real task receipt, sharing and once-only influence

Add task schema1→2 tables for immutable observed turn outcomes, receipt revisions, task participation/handover, explicit evidence/sharing decisions and per-world delivery acknowledgements. Validate v1 schema/rows before DDL and v2 after; preserve old tasks, notices, approvals and known IDs. An attribution that cannot be reconstructed remains unknown; do not invent historical participation.

Create a baseline receipt only from a native turn with an explicit `completed`, `failed` or `interrupted` status. `completed` becomes `turn_ended`, not `verified_result`. Unknown/missing statuses yield no successful receipt and remain inspectable/attention-required. Running/approval/idle-only updates cannot create an ended-turn receipt. Native notification and reconciliation feed the same transaction owner and unique task/turn outcome identity. Reconcile every returned terminal turn before the unchanged-task early return; observe discovered turns once, without issuing inference. A second equivalent notification/read returns the original receipt even if the task's current owner/status/revision later changed.

Record the managing owner and participation from actual Lina-managed turn/input/handover transitions. External mentions are not participants. The receipt keeps original task/turn/revision/attribution, while its model-facing projection excludes cwd, prompt, raw log, paths and internal identifiers. New receipt revisions refer to the prior receipt and preserve historical fictional events. Correction/retraction changes future evidence and adds a scoped correction observation; revocation invalidates future export and the publication evidence exposed to060.

Only trusted management actions can record owner confirmation or sharing. The existing protected local management boundary supplies actor authority; neither an ordinary task tool nor model output can claim that actor. `verified_result` requires an immutable owner-confirmation or registered verifier evidence record linked to the exact receipt/revision and authorized fields. Reported evidence remains reported. HTTP cannot invent a verifier identity or turn outcome. Add explicit expected revisions and request keys to these operations; same key/same payload replays and changed payload conflicts.

Keep source sharing unset by default. A versioned sharing decision selects target world IDs, category ID, permitted outcome/participant information and an optional explicitly supplied summary. No automatic task-summary model call. The source's durable outbox retains receipt revisions and per-world status until the world inbox commits and source acknowledgement succeeds. A restart/crash between those operations retries the same immutable key. Permission checks run both before export and world admission. Withheld rows retain reason/history and can be revisited on an explicit policy change; they are not falsely acknowledged as delivered.

Add `LifeInputV2` for verified application-owned work receipts, preserving exact v1 application rows. Its immutable source carries receipt identity/revision, supersession, participant attribution, permitted fields and sharing-policy proof. World schema5→6 adds current work-evidence projection/provenance and preserves input/outbox history. Only the trusted work bridge writes these inputs; model and HTTP step bodies cannot supply one. Duplicate delivery returns the old inbox receipt; a changed payload under the same key rejects. Current evidence follows the correction chain, while consumed input revisions remain part of accepted step history.

Use `LifeConfigInputV2`/`LifeConfigV2` with an explicit nullable `work` group instead of changing confirmed world-pack grammar for application consent. V1 bytes/behavior remain exact. Work policy rules bind category/outcome/attribution filters to a known family, explicit weight and optional required-match condition. Validate referenced families against the current pack. No built-in task categories, achievement/fatigue dimensions or default influence weights. Missing work settings disable work influence/export without disabling otherwise configured text simulation. A config change uses040 generation invalidation.

The selector reads frozen current permitted work evidence, records a private `work` contribution and applies any required-match condition after world constraints. Actor/director inputs receive only their authorized permitted work observation, never full task receipts or another agent's private summary. Newly applied inputs generate owned work/correction experiences and enter `consumedInputIds` in the same accepted step transaction; retry cannot duplicate experiences or growth. Past valid evidence may remain a configured eligibility condition after consumption, but only new input creates another experience. Reflection continues to validate its evidence and identity locks. The controlled fixture must show actual TaskManager completion→outbox→world evidence→changed next opportunity, not a hand-built success input.

### Shared persona and explicitly permitted ordinary recall

Add `sharedGrowth` to persona composition as a separate read-only option, present even with native dynamics. Keep the entire authored name/role/voice/profile and manual locks. Distinguish initial adaptable temperament from immutable identity: the validated current shared traits/habits/attitudes govern their adaptable axes. One core authority helper supplies the same precedence to the ordinary prompt and NPC view. No mutable copy goes into Dynamics or memory Engine. Both projections respect the current profile revision/manual state and the world binding's disclosure revision. `lina_persona_read(growth)` returns qualified current fields only, without private causes, entry IDs or hidden event rationale.

Existing learned Dynamics/preferences need model-facing reads that audit their exact sources through the journal resolver, and an apply-time check after every await. Preserve originals and explicit profile/style settings. Legacy or mixed-derived learned rows are withheld, not silently reclassified or deleted. Reversion cannot restore a disallowed derived row into model context. A new eligible ordinary observation must still learn even when old rows are withheld.

Version the existing world binding to add an explicitly selected nullable conversation recipient ID; old v1 bindings mean no ordinary event recall. Reuse `projectPublication` for ordinary recall: it already requires the agent to know the subject, an explicit agent→recipient disclosure and the recipient in publication permission. This avoids equating the user's author inspection with every agent's knowledge. A recipient must be supplied by trusted management/application identity; the model cannot select it. A null recipient exposes shared growth only.

Add a new conversation policy version carrying this recipient binding and source-policy version; retain v1 conversation's no-disclosed-LIFE restriction and v2 world-author semantics. The new policy permits `disclosed-life` only for the explicit read tool and current allowed publication projection. It does not automatically inject events. Binding/recipient/revocation changes alter scope digest and require012 native transition. An explicit recall with no allowed material returns a bounded empty result without hidden counts/IDs. Record even possibly delivered recall material before sending it; the affected conversation episode becomes mixed and its factual extraction is withheld. This creates a reachable mixed-episode test without weakening the old policy.

### Consumer persistence and read-time policy

Memory Engine2→3 adds source proofs to requests/receipts/records and rechecks journal policy before prompt construction, known-slot comparison, record merging, apply, snapshot/state/recall. Engine request ID remains a user-entry ID; never confuse it with a journal request ID. A correct quote from an ineligible source cannot validate an observation. Cached/old explicit records cannot outrank new eligible sources by bypassing policy. Preserve old revisions and errors for inspection.

Companion queue3→4 adds policy refs and `withheld` with a reason distinct from done/failed. Recheck scan, recovered pending/running/failed/done job paths, receipt shortcuts, immediately before provider dispatch and after response before apply. Policy revocation while the observer waits must reject the stale result. Withholding preserves attempts/history and does not block unrelated eligible episodes. Native preference and non-native reflection use the same rules for their entire prompt seed (history, existing preferences/dynamics and pending growth), not just the newest quote.

Context1→2 adds provenance for external summaries, summary ancestry, working state and cache versions. Native SummaryReceipt is versioned independently. Stage/activate/expand/archive/search/working/injection all preserve the union of source proofs and recheck it before ranking or limits. Working text with no trusted source cannot become ordinary model memory. Cache identity includes current source-policy revision/digest; stale observer/summary output is rejected rather than relabeled. Human history/inspection remains separately available. Cover `lina_history_search`, `lina_context_expand`, `lina_context_update`, `lina_memory_query`'s extra journal search, and status-tool context fields.

Honcho outbox1→2 and PartKey metadata add source proofs and a policy-versioned capture scope. Both pending send and unknown reconciliation re-resolve eligibility before network calls; keep withheld parts and delivery history. Validate metadata at parse/dedup boundaries, including chunk copies. `RecallResult.scope` currently copies local config and is not remote provenance. Therefore unqualified legacy/global recall is withheld and cached text cleared. Provide the explicit qualified ordinary-only namespace contract and test it with a local server; do not claim live remote filtering/namespace migration without an authorized qualification. Never delete old remote data or silently reuse it as ordinary memory. This does not disable local native ordinary learning.

## Expanded change map and B write scopes

The earlier path table remains the scope baseline. The source investigation adds these necessary owners and full field paths:

| Owner / paths | Creation → persistence → decoder → consumers |
| --- | --- |
| Main core `source-policy.ts`, NEW `source-policy-store.ts`, `source-policy-schema.ts`; MODIFY `protocol.ts`, `entries.ts`, `requests.ts`, `store.ts`, `index.ts` | Trusted request/native receipt → journalv2 policy/association rows → strict version/restriction parsing → shared source lookup and all derived stores |
| Main runtime `sdk-port.ts`, `sdk-events.ts`, `prompt-lane.ts`, `runtime-types.ts`, `runtime.ts`, `host.ts`, `session-app.ts`; Codex `context-policy.ts`, `session.ts`, `identity.ts` | Managed request correlation/context exposure → JSONL and journal before settlement → native/header/source decode → observer eligibility,012 transitions and ordinary activation |
| Task worker Codex `task-types.ts`, `task-schema.ts`, `task-store.ts`, `tasks.ts`; NEW `task-work-types.ts`, `task-work-store.ts`, `task-work-validation.ts` and receipt tests | Observed native terminal turn or trusted decision → tasksDBv2 immutable receipts/attribution/outbox → strict TaskManager work API → main bridge; no world/runtime/memory writes |
| Main world `life-types.ts`, `life-validation.ts`, `life-record-validation.ts`, `life-persistence.ts`, `authoring-types.ts`, `authoring-validation.ts`, `authoring-request-validation.ts`, `authoring-persistence.ts`, `views.ts`, `autonomy-types.ts`, `autonomy-source.ts`, `autonomy-persistence.ts`, `autonomy-step-records.ts`, `autonomy-replay.ts`, `autonomy-transition.ts`, `autonomy-views.ts`, `events.ts`, `schema.ts`, `store.ts`, `index.ts`; NEW adjacent `work-types.ts`, `work-persistence.ts`, `work-validation.ts` | Verified task projection+configv2 → inputv2/current evidence/worldDBv6/step source → exact old/new decode/replay → selection,owned experiences,consumption and later060 revocation proof |
| Main runtime `life/work-bridge.ts`, `fleet/codex-fleet.ts`, `fleet/life-runtime*.ts`, `fleet/task-routes.ts`, `fleet/life-routes.ts`, `world.ts`, `context-policy.ts` | Explicit sharing/binding/confirmation → task/world owners → status/error/request-key decode → signal-driven bridge and current ordinary recall; no external SNS |
| Main persona core `agents/persona.ts`, `agents/store.ts`, `agents/conversation.ts`; runtime `persona/hooks.ts`, `fleet/onboarding-routes.ts` and integration tests | Qualified growth/learned-source view → unchanged authored source plus derived proof lookup → projection validation → same ordinary/NPC authority and safe growth tool |
| Native memory worker `lina-memory/src/engine/*`, runtime `context/companion*.ts`, `persona/native-preferences.ts`, `persona/reflection.ts`, their tests | Journal resolver/source proof → Engine3/queue4 → audit/withheld/recheck → bounded prompt/state/recall/preferences; no shared journal/persona store edits |
| Honcho worker `lina-memory/src/honcho/*`, runtime `context/capture-scan.ts`, `context/memory.ts`, their tests | Qualified source/scope → outbox2/PartKey → chunk/reconcile/parser → send and qualified recall/hold; no live remote calls |
| Context worker core `context/*`, runtime `context/external.ts`, `coordinator.ts`, `native.ts`, `tree.ts`, `summarize.ts`, `receipts.ts`, `hooks.ts`, `injection.ts`, `tools.ts`, `memory-query.ts`, `tools/notepad.ts`, their tests | Source refs/working request → Context2 and summary/cache receipt → ancestry/filter validation → all model-facing context paths; no main session-app composition edits |

Workers may split focused helpers within these existing owned prefixes. Main freezes the common SourcePolicy/lookup, WorkReceipt, config/input and context-policy types before dispatch. Native, Honcho and Context workers share only these read-only contracts; task work can proceed alongside main journal work. Respect the host's concurrency limit and dispatch dependent lanes only after their inputs exist. Main inspects each diff and runs complete cross-store integration, not only worker unit tests. This P amendment authorizes these bounded delegations; it does not create extra work-phases or external authority.

## Verification plan grounded in current source

The immediately preceding `check-autonomy.py` ran `env LINA_AUTHOR_NATIVE_TEST=1 LINA_LIFE_NATIVE_TEST=1 bun test`, `bun run typecheck`, `bun run lint`, `bun run ci:build`, `bun run ci:validate`, document verification and diff check, all exit0 at3ff96e3. Root Bun discovery includes `packages/*/test`; root TS includes package source/tests and CI scripts; the separate browser TS check remains. These commands observe changed code, not new plan prose semantics. The owned document checker observes this exact decade document and its local links. New050 regression names below are future tests until B creates and runs their failing cases.

In addition to the existing acceptance table, trigger the actual unchanged-state reconcile shortcut with two unseen completed turns, a running-turn `completeTurn` call, changed ownership after a saved receipt, crash between delivery and ack, and stale confirmation keys. Source tests inject failure between policy and entry association/settlement, a forged raw ordinary label, mid-turn planned disclosure with lost acknowledgement, and a later no-new-recall turn in the same native epoch. Include many forbidden records before a valid result to prove filtering occurs before query limits. Run old v1/v2 fixtures through real migration/reopen with exact bytes and final-DDL failure rollback.

Final C includes actual management HTTP requests for explicit task confirmation/sharing, ordinary binding/recall policy and current status; malformed/oversized/Origin/Host/stale/retry paths and temporary installation teardown. The integrated story uses real TaskManager fake RPC plus real stores and scoped native serialization: receipt→work evidence→selected opportunity→growth→ordinary next request with native memory enabled→mixed recall withheld from actual Companion/Engine/context and local Honcho dispatch→reopen→no duplicate causal effect. Actual generated expressive behavior, paid providers, production Honcho qualification and rendered UI remain080 proof gaps, not fixture successes.

Enforcement is application-owned parsing/SQLite transactions and native transport source association (E7), with012 native-context fencing. Prompt wording is guidance. Privileged host/source/DB writers or a dishonest registered external verifier can bypass these assumptions; this is not hostile-host attestation or independent proof that arbitrary user prose describes real events. No wording claims an unbypassable fact detector.


## Shared signatures fixed before B dispatch

`lina-core/src/source-policy.ts` is SDK-independent and imports only core protocol/node primitives; memory consumers may import it by the existing workspace-relative convention. It does not depend on runtime or world types. Exact contract:

```ts
type SourceScope = "ordinary" | "life" | "mixed" | "unclassified_legacy";
type SourceMaterialKind = "shared-growth" | "disclosed-life" | "author-world";
interface SourcePolicy {
  version: 1;
  scope: SourceScope;
  sessionId: string;
  requestId: string;
  nativeEpoch: number;
  scopeDigest: string;
  policyRevision: number;
  contextReceiptIds: string[];
  materialKinds: SourceMaterialKind[];
}
interface SourceProof { entryId: string; policyRevision: number; policyDigest: string }
interface SourceEntry {
  entryId: string;
  role?: "user" | "assistant" | "tool" | "meta";
  text: string;
  timestamp?: string;
  sourcePolicy?: SourcePolicy;
  requestStatus?: "queued" | "accepted" | "settled" | "rejected" | "interrupted";
}
type SourceLookup = (entryId: string) => SourceEntry | undefined;
```

Missing `sourcePolicy` means unclassified legacy; it is never an ordinary fallback. Explicit unclassified policies retain a known request binding when proof is incomplete. All known policies require nonblank bounded IDs, native epoch/revision safe integers at least1, a64hex native scope digest, unique bounded receipt IDs and material kinds. Scope is constrained by the trusted session purpose and exposure lineage; `ordinary` cannot contain `disclosed-life` or `author-world`. Canonical ordering/digest is core-owned. Functions `parseSourcePolicy(unknown)`, `sourcePolicyDigest(policy)`, `isOrdinarySource(entry)`, `captureSourceProofs(entryIds, lookup)` and `sourceProofsCurrent(proofs, lookup)` provide common parsing and before/after-await checks. Capture throws on missing, nonordinary or conflicting sources; validation returns false for stale/missing/ineligible proofs. Persisted flags/proofs never replace the current resolver. Test fixtures explicitly create synthetic trusted policies; production content never uses those helpers as a promotion API.

The core journal exposes `sourceEntry(entryId)` and `requestSourcePolicy(requestId)`, plus trusted-owner methods to register/extend request provenance and append entry with association atomically. New policy history is keyed `(request_id, policy_revision)`; current request reference points to the last revision; entry associations carry owning request identity, not a copy that can lag behind. Exposure rows are immutable by receipt ID except the allowed planned→delivered outcome, with exact binding/material comparison. Existing journal request/entry/revision history stays intact. Replaying identical source writes is a no-op; changing source identity, deleting prior exposure evidence or weakening scope conflicts. Request status remains owned by `Requests`; source tables cannot create a fake settled request.

`SdkSessionOptions.sourcePolicy` is a trusted callback sink object with request/exposure/entry association methods implemented by the journal owner before native construction. `SessionPort.sourceEntryPolicy(entryId)` returns native-owned recovered association proof, never a value parsed from `EntryInput.raw`. Main will name callback packet fields in these exported types before consumer dispatch; consumers need only `SourceLookup`. Native transport associates the managed `PromptAdmission.requestId` before `turn/start`, not by user-text equality. The current Codex adapter rejects concurrent prompts and does not support steering;050 preserves that behavior. Any compatible queued port must supply exact associations or remain unclassified, and may not infer a missing request ID from the current turn. JSONL writes precede journal recovery callbacks; a failure after either write is safe to replay and cannot grant ordinary eligibility from a partial record.

`SessionContextPolicy` adds **version3 / purpose conversation** with `conversationRecipientId: string | null`, retaining all common v1 fields and a computed digest. V1 conversation/life and v2 world-author remain byte-compatible. Version3 is used for every managed ordinary session, including `worldId:null`; there `conversationRecipientId` must be null and binding/disclosure revisions0. With a world, only the explicitly bound recipient can recall. `BindingSelectionV2` and `WorldBindingV2` use **version2** and `conversationRecipientId`; existing unversioned selection and version1 persisted binding preserve their exact decoders and have no recipient. No implicit channel/user ID or publishing audience is selected.

Journal2, Engine3, queue4, Context2, outbox2 and world6 migrate only known prior schemas inside their existing transaction boundary; final schema/row validation occurs before commit, not on the next open. None of these version numbers refers to Honcho HTTP/v3 or native summary receipt version. The summary worker explicitly versions that receipt on disk and verifies both historical recognition and model-facing withholding. New consumers cannot silently fall back to `store.entry` when `store.sourceEntry` is required.


Settlement is a separate trusted fact from classification. `SourceEntry.requestStatus` comes from the journal association. Factual `isOrdinarySource`, `captureSourceProofs` and `sourceProofsCurrent` require `settled`, in addition to ordinary scope; absent status is unclassified for these consumers. They must not admit a currently ordinary but unfinished episode which can later disclose LIFE. The Context working-state path may pass the explicit optional third helper argument `{ activeRequestId }`, supplied by runtime, to include an `accepted` source belonging to that exact request. That override never permits life/mixed/unclassified/rejected/interrupted sources and is unavailable in model parameters or factual-memory adapters. Current working edits automatically union the active user entry's proof, cited sources, and the proofs of every retained old field, preventing a mixed current model turn from laundering new text through old ordinary citations. Later reads without the active override require settlement; any intervening mixed downgrade invalidates the working row. Empty working state needs no sources; nonempty generated working text does. Tests cover midturn ordinary→mixed, cancellation after a working edit, and settled ordinary progression.

Native correlation and persistence order is explicit: runtime first creates the Lina request; after binding a native epoch, the adapter writes a managed source-begin record and invokes the journal sink before sending `turn/start`. Existing bootstrap exposures in that epoch and new planned turn/tool exposures extend only the current request; old settled requests keep their frozen prior evidence. A native `turn/started`/start response maps the exact request to a turn; entry association uses that map. An item arriving before the turn identity is established is retained without ordinary eligibility until exact reconciliation. The adapter does not infer association from message text, role or the last active request. A managed entry is appended with its source association in a single journal transaction before runtime/host settlement collectors can inspect it. Recovery replays owned source records and entry associations before collectors start; source absence leaves entries inspectable and withheld. If runtime settlement beats an unresolved association, missing source remains withheld rather than assumed ordinary.

The first-unit012 statement that unrelated ordinary clients retain old behavior applied before050 composition.050 deliberately makes managed ordinary composition provenance-aware and may require a fresh native epoch on next authorized application start when legacy retained material has no proof; the Lina conversation and user-visible history remain. Direct optional adapter clients retain their old API but do not obtain ordinary memory eligibility. No running installed session is migrated during this task, and no old transcript is copied to the new native epoch as a fallback.


## A round1 synthesis and amendments

Two independent reviewers returned FAIL with two blockers each; all four blockers and their four named notes are accepted. The shared root cause was persisting a source label without closing every later consumer or ownership transition. `work-memory-a-memory-round1.json` and `work-memory-a-work-round1.json` record the specific roots, decisions and unaffected scope. No050source was written during this review. The following contracts supplement the field map before B; the same reviewers recheck them.

### Receipt/API finalization and revocation

`WorkReceipt.ownerAgentId` is nullable and `attributionStatus` is `known|unknown`; unknown has null owner and no claimed participants. Keep the effective observed outcome separate from `supersedesRevision` and `correction {kind:amend|retract,reason}` as fixed in the contract above. A retracted receipt is excluded from current eligible evidence, even if its historical outcome was verified. Each immutable revision points to the immediately preceding revision. The baseline native outcome and its original task/turn attribution cannot be overwritten by a confirmation, later owner change, or correction. Re-confirmation after a retract requires a new explicitly authorized revision linked to the same observed baseline, never an invented terminal status.

Sharing is not copied into immutable `WorkReceipt`: `SharedWorkFields` and `policyRevision` belong to each delivery projection and its separate sharing decision. `WorkReceipt.evidenceRefs` contains immutable references appropriate to that receipt revision. The task owner exports these exact operation names through TaskStore/TaskManager; helper types live in `task-work-types.ts` and strict decoders in `task-work-validation.ts`:

- `observeWorkTurn(taskId, turnId, status)` is internal to the native notification/reconcile transaction, accepting only explicit native terminal outcomes. It is not an HTTP or model operation.
- `workReceipts(taskId)` is read-only and returns all immutable revisions.
- `confirmWork(taskId, {receiptId, expectedReceiptRevision, requestId, evidenceRef}, authority)` records linked owner confirmation or a registered verifier result. `authority` is a trusted server-side port, never JSON. A model-reported `evidenceRef` alone grants nothing. Verification is linked to an explicitly observed completed turn; failed/interrupted baseline status cannot be changed into completed by confirmation.
- `correctWork(taskId, {receiptId, expectedReceiptRevision, requestId, kind, reason}, authority)` amends by dropping derived verification back to the unchanged observed native outcome, or retracts current eligibility. It never accepts a replacement native status or arbitrary participant IDs.
- `shareWork(taskId, {receiptId, expectedPolicyRevision, requestId, selection}, authority)` creates a separate sharing-policy revision. `selection` is null (revoke), or `{worldIds, categoryId, shareOutcome, shareParticipants, summary}` with explicit booleans and nullable user-supplied summary. No model-generated summary or default category/audience. Source attribution is retained privately regardless of whether participant names are permitted in model output.
- `pendingWorkDeliveries()` returns source-owned per-world durable operations. `acknowledgeWorkDelivery(deliveryId, payloadDigest)` acknowledges only the exact committed world receipt; failed/withheld attempts remain inspectable and are not successful acks. A work-change subscription wakes the main bridge and startup drains the same queue; no product polling cadence is invented.

Every modifying operation uses same-key/same-payload replay and same-key/changed-payload conflict, including authority identity in the canonical operation digest. IDs and revisions have bounded strict parsing; source task/current owner authorization is checked by the trusted task owner. Runtime routes supply the local-management authority. They do not prove a biological human's identity. New `/api/tasks/:id/work` read and `/api/tasks/:id/work/(confirm|correct|share)` writes reuse LIFE's Host/Origin/method/body guards; expected receipt/policy revisions are separate from the task's display revision. Ordinary task tools do not register these management writes.

Policy revisions and causal receipt revisions are independent. A delivery key binds `(receiptId, receiptRevision, policyRevision, worldId, operation)`; the one-time experience key binds `(receiptId, receiptRevision, worldId, agentId)`. A share-only change or re-share can update permission/evidence but cannot create another work experience or growth. A material correction revision creates at most one owned correction experience. Unknown attribution cannot create an agent's personal work experience by guessing the task's latest owner.

Retargeting/removing a world, retracting evidence, and `work:null` deliver restriction records to every previous recipient even when new exports are disabled. Do not strand revoked evidence behind the current export filter. World6 persists current work-evidence revision and restriction history. Main bridge synchronizes restrictions before preparing a step; frozen step source includes that revision. Any change invalidates a pending step, fences every following model send and final commit, and preserves already incurred model accounting. Current source/destination policy is checked again after awaits. Permission races cannot be repaired by writing a newer consent label onto old frozen data. Historical step replay uses its original frozen evidence to validate what happened then, while current eligibility/recall use the latest restriction.

Accepted work-derived events/claims/experiences retain private work-evidence ancestry. `projectPublication` and ordinary explicit recall check that ancestry against the current restriction projection before exposing derived material;060 will use the same check for feed withdrawal. Revocation leaves historical simulation records intact but prevents new public/ordinary disclosure of their restricted derivation. Tests must revoke after delivery and during a deferred model call, then reopen and exercise both ordinary recall and no-duplicate re-share. `authoring-request-validation.ts`, `authoring-persistence.ts`, `views.ts` and `autonomy-persistence.ts` are explicit main-owned consumers of configv2, work revision and derivation checks.

### Managed notepad and retained working text

Add source-aware managed notes to the Context2 schema, owned by the Context worker, and connect `runtime/tools/notepad.ts` to them. Each note binds a generated note ID, exact text, current journal request/entry, and immutable creation/source ancestry; active creations use the pending→finalized protocol below. Content cannot supply this authority. Missing current request proof refuses managed append; known mixed text may be retained as withheld evidence, never returned as ordinary note content. Reads use the same current source resolver before limits, with no raw-file fallback and no echo of withheld text in errors/details. Scope downgrade/revocation invalidates a note's eligibility. Keep the existing plaintext `notepad.md` unchanged for human inspection; imported legacy bytes have no managed ordinary eligibility. Main changes only session-app wiring; the Context worker owns its store/codec/tool/tests.

Mixed append→clean epoch/revocation→process reopen→notepad read must return no secret. Ordinary accepted append becomes eligible after settled evidence and remains readable after reopen. Context working partial updates preserve source ancestry for every retained old field; changing `sourceEntryIds` cannot relabel retained mixed text. A full explicit replacement may discard prior text and its ancestry, provided all replacement content has eligible current/cited sources. Partial ordinary edit of a mixed row stays withheld. This is a first-party memory-tool guarantee; separately authorized arbitrary host file access remains the declared privileged-host bypass.

Main also connects onboarding preview's `getPreferences().items` caller to the qualified preference view and revalidates source proofs after awaited preview generation before returning/applying any derived result. Explicit authored conversation settings and user-approved onboarding answers remain their existing distinct trusted sources.

### Honcho generations and explicit namespace qualification

The Honcho worker adds optional `HonchoConfig.ordinaryNamespace` with exact fields `{version:1, ownerBotId, generationId, workspaceId, sessionId, userPeerId, observerPeerId, sourcePolicyVersion:1, qualificationId}`. It is an explicit operator selection; no namespace/generation/qualification is guessed or enabled by default. Main adds its transport through existing config parsing and `Fleet.memoryConfig`, which must not overwrite these selected IDs with `lina-...`/`agent-...`. `HonchoIdentity` for initialize/capture/recall uses the selected qualified tuple consistently. A namespace must be isolated from the old workspace/peer aggregate, not merely a new session name pointing at old memory; local qualification tests cover this. An arbitrary qualificationId or a locally copied `RecallResult.scope` is not remote proof: the configured adapter must return and validate the namespace/source-policy contract before model recall is usable. Missing capability remains explicitly unavailable, with no provider call counted as proof.

An immutable local generation owner binds the Lina BotBinding, selected identity tuple, generationId, sourcePolicyVersion and qualificationId. The selected generation's outbox path is derived from the canonical owner digest beneath the task/session root, never a caller path. Reopen validates the full owner as today. The old `honcho-outbox.sqlite` stays bound to its original destination, retaining pending/unknown/delivery history; it is not retargeted, deleted or silently resent. Its known schema may migrate to record withheld reasons, but no old ambiguous part is copied as qualified content. Old configured direct adapter paths remain recognized for inspection without qualifying ordinary recall. New eligible journal entries enqueue into the selected generation only after source checks, using the same namespace for initialize, write and recall; generation changes clear cached recall. Local fixtures open old pending/unknown rows, select a synthetic qualified destination, capture and recall a new eligible episode, reopen both generations and prove zero old-destination resend/retarget and zero unqualified recall. No production remote namespace is created during this task.


The Honcho config owner is `lina-memory/src/honcho/config.ts` (inside the Honcho worker scope), including `validateHonchoConfig`, `publicIdentity` and `parseHonchoEnv`. Explicit operator transport uses optional `LINA_HONCHO_ORDINARY_NAMESPACE_JSON`; malformed or incomplete namespace input rejects, and it cannot replace required base connection settings. No environment is changed by the implementation task. Main owns `Fleet.memoryConfig`/`initializeMemory` and session-app generation-path wiring; they use the validated selected identity rather than manufacturing independent IDs. The stock HTTP/v3 client must not claim an undocumented remote provenance capability. A qualified adapter option supplies the explicit namespace qualification/recall proof contract; absent a validated capability, model-facing remote recall stays unavailable. Both qualifier success and failure are exercised with local fixtures; live qualification remains separately unperformed.


## A round2 synthesis and closure contract

Memory reviewer confirmed the prior four memory amendments and found two new construction issues: fleet-global namespace lacked an agent owner, and active context artifacts pinned a policy revision before tool exposures finished. Both are accepted in `work-memory-a-memory-round2.json`; this changes neither factual eligibility nor provider authority.

Qualified `ordinaryNamespace` additionally requires `ownerBotId`. Generation ownership, qualification request/proof, capture metadata and recall proof include that owner, which must match the actual BotBinding. A different agent cannot inherit, relabel or reuse its qualified destination. Main adds explicit `Fleet.options.honchoByAgent?: Record<string, HonchoConfig>`; `memoryConfig(id)` chooses that agent's exact entry first, otherwise the existing global config. A map key must match namespace.ownerBotId. A mismatched global namespace is not used for that agent and reports unavailable qualification; do not derive replacement session/observer IDs. Ordinary unqualified legacy config remains available only under its already declared source/recall restrictions. This map is programmatic fleet configuration, not an implicit product default; the existing environment option can explicitly qualify one owner, while no multi-agent remote namespace is silently provisioned. Tests construct a two-agent fleet with explicit per-agent configs and capture the distinct initialized/capture/recall identity tuples, reject wrong-owner proof/config, and also test the single global qualified namespace cannot leak to a second agent. The Honcho worker owns namespace type/validation/qualification functions; main owns fleet map selection and passes the actual owner to the client/bridge qualification boundary.

Pending context artifacts (working rows and managed notes) separate immutable creation evidence from final ordinary eligibility. At write, persist creating request ID, native epoch/scope identity, creating user entry ID, the creation policy snapshot and existing cited/inherited **settled** dependency proofs. The active creation request is a durable pending association, not a fabricated final proof. Store status `pending|finalized|withheld` with a reason; any active-source text retained by a partial update retains that association too. A tool's later empty/shared-growth exposure may advance the creating policy without making a valid note permanently stale.

On journal request settlement, a trusted `ContextStore.finalizeRequest(requestId)` path resolves the same request/entry/epoch identity and its final ordinary policy, verifies all prior cited/inherited settled proofs are still current and ordinary, and writes a separate final proof in one ContextStore transaction. Keep the original creation record and revision history. A mixed/life/unclassified/interrupted/rejected episode becomes withheld; no new ordinary label is inferred from content. Runtime calls this after durable settlement and recovery scans pending associations before context model reads; a crash between journal settlement and ContextStore finalization is idempotently recoverable. Reads only use finalized eligible artifacts, except a runtime-supplied exact current-request view may inspect its pending ordinary working state with all live dependencies checked. This narrow pending-artifact protocol does **not** refresh or promote general stale summaries, memory observer outputs, or already mixed notes. Full replacement creates a new creation record; it cannot silently discard inherited text ancestry from a partial update.

Add tests ordinary append/update→additional empty/shared-growth tool exposure→settle→reopen (readable), mixed exposure after append (withheld), active cancellation (withheld), changed cited settled proof (withheld), and crash immediately before/after final proof write (one finalization). Main owns durable settlement notification/wiring, Context worker owns pending records/finalization/recovery and note/working tests. The source helpers' ordinary factual default remains settled-only.


## B checkpoint — common source journal

The common source contract is implemented in core `source-policy.ts` and the adjacent `source-policy-origin.ts`, `source-policy-schema.ts`, `source-policy-store.ts`. Journal schema2 exposes `sourceEntry`, `requestSourcePolicy`, `recordSourceExposure`, `registerRequestSource`, `extendRequestSource`, and atomic `appendSourceEntry`. Core index exports the shared consumer types/helpers. The native transport sink uses SourceRequestOrigin (trusted purpose/session/request/epoch/scope/initial receipts) and SourceExposure; scope is derived from retained receipts, not accepted from model/raw labels. Actual native admission/settlement wiring is still being implemented.

First proof: missing module RED, then5 policy tests/44 assertions GREEN; journal API RED4 failures, then24 source+existing entry/request tests/170 assertions GREEN including exact v1 schema fixture and real reopen. Source helper/store scoped Biome has no errors (existing literal-key infos remain). The first root typecheck caught two local typing errors, which were corrected, and also observed intentionally incomplete worker tests; a complete root types PASS is not claimed yet. No050 C proof or provider-quality proof is implied.

Four B workers are active with the audited disjoint scopes: task receipts `01a07e8b-1f0c-75a0-907e-21f16ea9d6d1`, native memory `01a07e8d-180d-7eb3-8ea9-49841c10b30b`, Context/notes `01a07e8d-19d1-7e82-a0ea-36af8752f904`, Honcho `01a07e8d-97d9-77f0-8838-e19bbcf6499b`. They use fresh contexts and the host's default inherited model configuration; family-level independence was not established. Main owns integration and will inspect returned diffs/proof before claiming completion.


### B delegation amendment — native journal transport

With the shared journal API implemented, delegate its bounded transport caller chain to a fifth worker so main can implement world work evidence while task/memory consumers proceed. The worker owns Codex `context-policy.ts`, `session.ts`, `identity.ts` and new adjacent source-provenance helper; runtime `sdk-port.ts`, `sdk-events.ts`, `prompt-lane.ts`, `runtime-types.ts`, `runtime.ts`, `host.ts`, `session-app.ts`, `context-policy.ts`, and their focused source/native tests. It owns no core journal, task files/exports, Context/Companion/Honcho internals, world, persona or fleet files. Main does not edit those owned files until the worker hands them back; later world/persona composition changes are sequential integration. The new worker implements exact request correlation, managed source sink/recovery, ordinary policyv3 composition, pending context finalization wiring and source-aware common services. Keep the existing LIFE activation guard until main's world+persona integration demonstrates the complete boundary. This is a split of already audited main-owned code, not new behavior/scope/authority. Main still verifies cross-store behavior. No source files are shared for concurrent writes.


### B integration amendment — learned store provenance

The native-memory worker completed Engine3/queue4 but its runtime layer needs the audited qualified AgentStore/ConversationStore APIs. Transfer `lina-core/src/agents/store.ts`, `conversation.ts`, new adjacent provenance helpers/schema support and their focused tests from main to that same worker, which already owns the dependent preference/reflection callers. Main keeps persona prompt composition, shared world growth and fleet/onboarding caller integration. This is non-overlapping ownership; no other worker edits these stores. The worker implements modelDynamics/modelPreferences, persisted complete-prompt proof receipts, current eligibility and safe apply/revert/candidate confirmation while preserving raw inspection/history. Optional legacy APIs do not grant model eligibility. Source core journal remains main-owned and may gain independently reviewed invariant fixes. This resolves an implementation dependency within already audited050 scope; it does not defer ordinary learning or change authority.


### B checkpoint — task-source mapping and remaining world owner

Main local commits: `117b49c` source journal, `04801a2` work configurationv2, `0733a63` autonomous step decoder for configv2. The step decoder test first had a missing test entropy argument (retained separately), then reproduced the intended strict-field failure; the corrected decoder passed8 tests/36 assertions with real reopen. The core source review subsequently found three real gaps; J1 user correlation, J2 receipt originating-request ownership and J3 legacy CHECK constraints are repaired with RED5pass/3fail→GREEN27pass/181assertions. Same reviewer is rechecking; no full050 approval is claimed. Current main scoped strict TypeScript passes (`work-memory-main-types5.log`).

`LifeInputV2` parsing and narrow `WorkInputSource` now preserve deliveryId, operation, sourceDigest, policyRevision, permitted fields and receipt provenance. Raw verifier references are replaced with evidenceDigest; no task cwd/prompt/logs enter the world projection. Its receipt has receiptId/receiptRevision/supersedesRevision/taskId/turnId/taskRevision/original owner+participants/attribution/outcome/correction. The generic application input port rejectsv2; the trusted work-bridge admission owner is still pending. Source parser/admission RED cases and27 LIFE storage tests/134 assertions passed (`work-memory-world-input-green2.log`). These latest world input edits are not committed yet.

The completed task lane exposes workDeliveryCurrent for both upserts and restrictions, workProofCurrent for eligible upserts only, explicit retry/attempt/ack and subscribeWork. The bridge must distinguish these; a valid restriction is intentionally not eligible work. `work-memory-task-integration.md` is the exact API handoff. Source manifests20 task/25 Honcho/30 Context files were matched by main (`work-memory-worker-manifest-check.json`); integration and independent reviews are still pending. Task/Honcho workers are closed after their source/API return. Context worker is finishing metadata handoff; native-memory worker now owns qualified persona stores, transport worker owns native/SessionApp.

Next world work is still required: World DB6 current evidence/restriction history, one-time work experience keys, event/claim ancestry and current disclosure checks; frozen work evidence/permission revision in autonomous step preparation/send/commit, selector contribution, scoped model observations and owned once-only experiences; bridge plus actualTaskManager-driven scenario and management routes. Preserve old accepted step bytes and use their historical frozen source during replay. Version any changed frozen step/source envelope explicitly rather than silently assuming old rows contain current work state. New incoming permission delivery keys include policy revision; causal experience keys exclude it. A first received source receipt can have a later revision because unshared intermediate source revisions were never exported; validate its live source proof and monotonic current evidence without inventing missing world history. Destination work configuration changes must fence pending steps and current derived disclosure even when there are no new source outbox operations, with re-enable/retry unable to duplicate experience. These are pending integration details, not implemented proof.


### B review amendment — final delivery checks

Independent Honcho/Context review found a source-change window after async validation: pending POST and memory-query tool answers could outlive their frozen proof through an outer await. Both findings and the direct undefined-field working replacement issue are accepted in `work-memory-context-honcho-review-round1.json`. Source checks must run at the last synchronous dispatch/serialization boundary, not merely inside an awaited helper. The existing workers repair their scopes; the transport worker additionally owns Codex `host.ts`, `rpc.ts` and a focused guarded-response helper/tests to carry an SDK-internal synchronous `LinaToolResult.beforeDeliver?: () => void` check through native result conversion and invoke it immediately before writing a response. This guard is trusted code capability, never model/imported JSON metadata and never serialized into content/details. Memory query retains original frozen proof and checks after its own wrapper await as well as through that final guard. This is the actual enforcement layer for the existing source boundary. Privileged host code remains outside the threat model.

### B bridge delegation amendment

The source task worker has completed its bounded repair and the original independent reviewer is rechecking it. Delegate the already planned runtime work bridge and narrow protected task-work management routes to a fresh worker. It exclusively owns new runtime `life/work-bridge.ts`, new `fleet/task-work-routes.ts`, task route dispatch wiring in `fleet/task-routes.ts`, and new focused runtime work tests. Main retains world persistence/selection/ancestry, fleet lifecycle installation, persona and native integration. The worker uses the current TaskManager work API without changing task source contracts and WorldStore.admitWorkInput/workEvidence; it proves source outbox → world commit → acknowledgement and actual TaskManager recovery, retry and restriction behavior with temporary databases. Existing HTTP server authorization remains the protection boundary; no JSON authority or model tool is added. This decomposition changes no capability, product setting or external authority.

### B task attribution root-cause repair

Independent task review round2 confirms T1/T2 fixed but reproduces T3: a rejected steer remains an unresolved input and blocks receipts from later independently accepted turns. Main reclaims task source ownership after two failed review packets. Persist dispatch targeting and distinguish definitive rejection from ambiguous transport loss; only relevant unresolved inputs may defer a terminal receipt. Preserve one-time baseline publication and historical attribution audit. No task model/tool authority is widened.

### B context guard root-cause repair

Round2 passes H1/C1/C2 but finds C3: pending callbacks continue using the pending exception after the artifact finalizes. Main reclaimed the narrow artifact guard after two failed review packets. Delivery now reads current artifact status and uses its persisted final proof; a previously captured finalized proof remains frozen. Actual tool/journal RED and GREEN evidence is recorded before same-reviewer closure. No proof is refreshed from current policy.

### B world work integration checkpoint

New stepv2 freezes current work evidence and a separate ancestry snapshot; old stepv1 bytes and outcomes are preserved. Current receipt/policy/config changes invalidate pending steps and never erase dispatched accounting. Private selector contributions and required matches use configured rules; model envelopes include only the actor's permitted fields. Experience keys exclude sharing revision and are committed with input consumption and validated work sidecars. A model-free clock opportunity receiving work records a `work` outcome with a neutral activity, using an existing recipient scene, then later social reflections may cite the owned observation. It does not create an unconfigured cadence or provider call. Without a placed recipient it defers that observation. Historical accepted inputs are never reapplied.

Work ancestry conservatively retains available permitted prior work causes and work supplied to step model recipients, plus new work observations. This can withhold a later derivative more broadly than minimal semantic causality; it never claims semantic dependency extraction. Current disclosure filters cover claims, facts, experiences, goals and scenes as well as publication events. A restriction carries only a neutral correction reference; old summaries/private correction reasons stay unavailable, while the neutral retraction can remain visible. Permission revisions also advance for corrected receipts with unchanged sharing policy. Ordinary wrappers still need wiring to these store-backed checks.

The first integrated world regression passed329 tests/1477 assertions. Actual v5→6 migration preserves accepted legacy step/source/outcome/receipt bytes and rolls back failed final pragma. Later tests cover corrupted work state/history/once-owner/ancestry, work-family orphan rejection and correction visibility; logs include fixture-only failures and their fixes rather than concealing them. New world edits are still awaiting independent review. Source reviewer J1/J3 closed; remaining J2 staged receipts are now checked before registration, with RED and GREEN before final recheck. Task T3 root-cause fixes pass57 tests/268 assertions and scoped TS. Context C3 original pending callback now switches to stored final proof and passes actual tool/journal tests; same reviewers are closing these repairs. These focused results do not close050C or authorize activation.

### B fleet integration delegation

Source journal J1/J2/J3 and Context/Honcho H1/C1/C2/C3 now pass independent correctness review; reported formatting was then fixed. Delegate the existing fleet Honcho selection/qualification and onboarding learned-source caller integration to a fresh worker owning `fleet/manager.ts`, `fleet/server.ts`, `fleet/onboarding-routes.ts` and focused new tests. Main retains session-app/native activation, persona/world behavior, installed lifecycle and bridge installation; bridge worker retains task route owners. This bounded move completes existing audited caller work; no default namespace, policy, provider spend or installation is authorized.

### B persona and ordinary binding checkpoint

Core `composePersonaPrompt` now keeps complete authored biography/appearance alongside name/role/voice, and separates initial temperament from current permitted adaptable behavior. `sharedPersonaBehavior` is the common allowlist/authority helper; NPC caller integration remains pending. Ordinary persona hooks read qualified dynamics/preferences, include shared growth even with native dynamics, and attach frozen source/shared-profile guards to prompt/tool delivery. Safe growth output omits source IDs/private causes; focused persona tests pass. WorldBindingV2 carries an explicitly nullable conversationRecipientId, preserves v1 bytes/no recall grant and survives reopen. Work permission changes advance existing binding revisions so native context scope can invalidate previously retained material; duplicate work deliveries do not advance them. Ordinary recall/management and activation are still pending.

Independent world review accepted W1/W2/W3: the frozen work revision now binds the complete admitted input list and historical consumption state, already-consumed inputs cannot become new experiences from config-only changes, and old input fixtures narrow v1 explicitly for strict TS. RED→GREEN6 tests33assertions precedes same-reviewer recheck. The standalone activate helper now requires an explicit managed notepad owner before tools/listeners; four tests pass with task-specific temporary contexts. Root TS passed after caller repairs, while browser TS revealed a type-only dependency on node:crypto. Shared source interfaces are being moved to a pure types leaf; no browser polyfill or Node dependency is added.

### B final provider dispatch owner

Fleet onboarding now forwards a frozen beforeDispatch callback, but the central authoring/auxiliary provider interfaces do not yet carry it to the final fetch owner. Delegate the narrow OpenCodex service/complete/client propagation and pure runtime service type declarations to a new worker. Main retains caller source-proof capture; native-memory worker owns Companion/preference/reflection callers, and native transport worker owns native RPC. This implements the already audited actual dispatch boundary without introducing provider calls, namespaces or new defaults. Source callbacks are trusted in-process capabilities, excluded from payload JSON, and run immediately before fetch after owner-side awaits. Failed guards produce no provider request. Actual paid/remote compatibility remains unqualified.


### B installed lifecycle and ordinary composition checkpoint

The independent core W1/W2/W3 repair review passed56 focused tests,80 autonomy regressions and six SQLite probes. Native transport handoff passed146 tests/743 assertions; final provider dispatch handoff passed99 tests/410 assertions. These are bounded packets, not the final050 verdict.

The installed fleet now awaits TaskManager restore before discovering retained LIFE worlds, subscribes/drains its work bridge before scheduler start, polls before freezing a step and detaches/drains before closing the task source. A trusted source-side check runs before each effect and through the native LIFE model selection callback at final outbound. Failed restriction delivery cannot extend an old upsert's authority. Focused installed/source/runner regressions passed22 tests/106 assertions, including reopen and no duplicate experience.

NPC input now uses the same shared behavior allowlist/authority as ordinary persona. Ordinary apps receive a lazy world source, so binding changes apply to an already opened app and fresh installations create no world. Explicit recipient recall produces material references from the actual tool result; no automatic event context is added. If source-side sharing is stale, the effective recall recipient becomes null and native scope changes while shared behavior remains available. This deliberately narrows recall until the restriction reaches the world; it does not erase history. Initial bootstrap now carries a frozen persona/source guard to actual thread/start or resumed instruction injection.

Actual SessionApp acceptance currently exposes a normal world-read response being blocked before tool completion. Investigation and independent native review remain in progress; the activation guard has been replaced only in the isolated implementation under test. Do not claim ordinary recall acceptance until this regression passes. Managed binding API and final integrated checks remain required before050C.


### B native acceptance and independent review repairs

Actual ordinary SessionApp tests now pass in both disabled and native-memory modes: configured shared persona is sent, event text is absent before explicit recall, permitted recall creates a mixed episode, that provenance survives native reopen, retained exposure remains mixed, and recipient revocation rotates to an ordinary native epoch. Mixed episodes make zero observer calls; the subsequent ordinary episode resumes native observation. The initial false failure came from a fixture that omitted the actual user item until completion; the test now delivers it before a tool request, matching the runtime ownership contract.

Independent native review found two real defects. A revoked tool body could reach tool_execution_end before its final guard, so the original proof now guards execution-history publication and every awaited handler boundary as well as RPC delivery. Concurrent prompt preflight could assign two requests to one source owner; admission now reserves synchronously before the first await and releases in finally. RED/GREEN probes cover both. This moves early failures to a normal failed-tool response while retaining final RPC rejection for later races; prior regression expectations were updated to that behavior without allowing private content. Same-reviewer recheck is pending.

Full tests initially reported2206 pass/35 skip/6 fail. Four failures expected the old late-only guard timing and now pass with the new earlier boundary. One actual migration regression rejected the repository's historical pre-authored Agent0 schema; exact schema validation now recognizes that prior shape before adding its receipt table in the existing migration transaction. One old context test wrote an unclassified working capsule outside a creating request; it now uses a qualified synthetic episode and finalization. Both focused regressions pass. Root and browser strict typechecks pass. The latest full world regression passed345 tests/1542 assertions.

Independent native-memory review found N1 incomplete intermediate episode filtering and N2/N3 truncated proof-sidecar integrity gaps. The original implementation worker is repairing those bounded files while main retains session/fleet/legacy-schema ownership. They are required050 blockers, not deferred work. Successful component tests do not yet close050 or the overall roadmap.


### B final destination and legacy request boundary

Independent world/persona/binding review is now PASS after preserving v1 serialized input/system prompts exactly. Only new stepv2 adds shared persona authority. A checked-in1983-byte director-envelope fixture comes from committed040 serializer3ff96e3; current code reopens a v5 SQLite file containing that model receipt and preserves both model/step JSON bytes through v6. The reviewer compared the fixture with its independently retained old request and confirmed both serializer revisions match. No Git executable/history is needed at test runtime.

Independent installed review found I1: source-side work approval alone did not protect a model call after destination work:null. Final model selection now rejects terminal/stale steps and compares the frozen destination config, work snapshot, world/life revisions and identity before checking source authority and route. The actual local gateway regression proves the work text was in the admitted request, then destination restriction produces HTTP400, zero upstream attempts and zero receiver requests. Completed model receipts retain their replay-only path without new selection or inference.

Native transport/provider review now passes both history-publication and concurrent-admission repairs, including normal subsequent requests, failure cleanup and initial/resumed bootstrap negatives. Main verified the supplied review hashes before saving the PASS packets. Native memory N1/N2/N3 remains the outstanding implementation/review packet.

### B complete episode and receipt integrity repairs

`DurableStore.sourceEpisode` reads the entire user/assistant episode, including unclassified intermediates and trusted late request members, in one consistent read scope. Consumers qualify that complete set before applying prompt limits; lookup outages preserve retryable work instead of becoming permanent rejection. Actual temporary-file reopen and injected lookup failure tests pass. Companion now uses this journal-owned query directly, preserving its bounded forward scan.

Engine records bind to their original processing receipt and full proof set. New preference and reflection receipts commit the original input and complete ancestry; truncated proof sidecars fail read/startup validation. Historical unbound receipts remain byte-preserved and model-ineligible. This is projection-integrity checking, not authentication against a privileged writer changing every receipt. No secret or new SQL version was introduced for these repairs. The native-memory worker delivered40 matching file hashes with193 passing tests/890 assertions, root/browser types and scoped formatting; same-reviewer N1/N2/N3 recheck is pending.

A separate manual HTTP invocation exercised14 binding scenarios against actual AgentFleet/WorldStore: explicit recipient, growth-only withdrawal, unbind, stale revision, hostile origin, forged authority, missing agent/recipient, unknown query/method, revoked installation owner and real reopen. All expected responses passed. Zero agent sessions were opened; the listener and temporary state were removed. Evidence is `work-memory-binding-qa.ts` and its JSON output under the task evidence root.

The second native review passed the original N1 omission, N2/N3 corruption and rollback probes, but found one remaining await race: preference/reflection callbacks checked saved policies without checking newly added episode members. Main added9 failing dispatch/commit/history/recall cases, then made both final guards re-resolve every user episode in the original captured prompt ancestry. Existing proofs are never refreshed to accept the new member. After this four-file repair,25 focused tests/88 assertions and76 affected runtime tests/332 assertions pass, as do root/browser types and the build. The preceding unchanged baseline passed2,269 tests/11,162 assertions with both isolated native flags; that whole-suite result precedes this last repair and is combined with its focused evidence, not relabeled as a new whole-suite run. Independent recheck remains required.


## Final verification and next unit

Independent source/task/world/persona/binding/transport/provider/native-memory reviews pass. The final native reviewer reran the original Companion race: zero dispatches, no preference receipt, empty model preference view. Its11 dispatch/commit/ancestry/recall/normal cases pass, with four repaired file hashes pinned and46 other reviewed files unchanged. Earlier FAIL evidence is retained alongside `work-memory-native-review-final.json`.

Evidence is scoped to the task-owned checkout and temporary state. `work-memory-check.json` combines the2,269-test native-enabled baseline with76 affected tests after the final four-file repair, exact source hashes, root/browser types, lint (eight warnings), actual web build and read-only CLI smoke, CI metadata and document checks. Manual binding HTTP covers14 scenarios and teardown; task/world bridge and final gateway/source boundaries have separate actual SQLite/HTTP probes in their review packets. No live model quality, qualified external Honcho installation, real user memory migration, UI, feed or image-delivery claim is made. No services from these checks are intentionally left running.

The rejected hypothesis was that unchanged saved source proofs alone establish unchanged episode membership. New members need an independent membership read, and the original captured set must remain fixed across awaits. Receipt hashes protect projection consistency; they do not authenticate an entire database against a privileged writer.

Continue060: scoped LIFE posts, replies, reactions, reshares and finite causal inputs, reusing the world disclosure/work-authority boundaries and040 execution owner. Publication/images/UI remain required successor work; do not mark the overall LIFE goal complete.
